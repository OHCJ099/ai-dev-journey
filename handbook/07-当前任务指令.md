# 当前任务指令（总指挥下达 · 2026-09-18 23:40）

> 用法：子教练按 `.hermes.md` 启动协议自动读到本文件。总指挥每轮重写，子教练只执行不修改。
> **规则（handbook/06）**：每次验收通过后必须重写本文件，否则「存在就以它为准」会一直派已完成的任务。

=== 复制开始 ===

【总指挥验收结果 · W2 第 1 格】**分层与行为全过**（新旧 `cli.py` 喂同一串输入 `diff` 完全一致；破坏性输入 13 项不崩；`storage`/`core` 无 `print`/`input`）。**尾巴四项未收，本轮全部收掉。**

## 本轮任务：W2 收尾（四件事，按顺序做，每件做完 commit）

**这在大纲哪里**：`ROADMAP.md` W2「函数、模块分层、类型注解、`pathlib`、`.env`、`httpx`」。**以后用在哪**：① 代码检查（lint）是每个项目的 CI 门槛，以后 RAG 项目的代码也要过；② 错题本是面试素材库 —— 面试官问「你踩过什么坑」，你翻 `gotchas.md` 就能答；③ 参考文件归档是工程习惯（`week03/` 存 W3 的参考答案）。

### 第 1 件：跑通 lint（15 分钟）

现在有 3 个错，先看清再改：

```bash
uvx ruff check week01/
```

- `week01/contacts/cli.py:1` → **I001 import 未排序**（你要自己改，不许 `--fix`）
- `week01/chat_cli_reference.py` 的 2 个错 → 第 4 件会把它移走，先不管

**改法（讲清规则再动手）**：import 要分两组，组内按**字母序**；`import json`（标准库）一组，`from storage/core`（本项目）一组，组间空一行。同一行里名字也按字母序，且**常量优先**（大写在前）。所以 `from storage import load_contacts, save_contacts, DATA_FILE` 是错的，`DATA_FILE` 要提到最前。

**验收**：`uvx ruff check week01/contacts/` → `All checks passed!`，贴输出给我。

### 第 2 件：建错题本（30 分钟）

新建 `notes/gotchas.md`，格式照 `notes/README.md`（**报错原文 1-3 行 → 原因一句话 → 解法一句话**）。

素材现成，从 `logs/2026-09-18-日报.md` 和 `logs/2026-09-17-日报.md` 里抄这 7 条：

1. `ValueError: too many values to unpack (expected 3)` —— 一行输入了 4 条命令
2. `IndentationError` —— `append` 贴到 `elif` 分支下（**缩进=分支归属**）
3. f-string 漏 `f` → 打印出字面量 `{name}`
4. `NameError` —— `except` 后漏 `continue`，`del_index` 未赋值
5. `TypeError` —— `print` 写在函数外，import 时即执行（**犯过 2 次**）
6. `IndexError` —— `delete_contact` 边界放宽一格
7. `if text == None` 判不出空列表 —— 应为 `if not text`（`[]` ≠ `None`）

**要求**：每条必须用**你自己的话**写「原因」和「解法」，不许抄日报原文。写不出来那条就问我。

**验收**：7 条齐 + 每条三行格式，贴文件给我看。

### 第 3 件：删旧文件（5 分钟）

`week01/contacts.py` 已被 `week01/contacts/` 三层版替代，且行为已证明一致。删掉（git 历史里永远留着，随时能找回）：

```bash
git rm week01/contacts.py
```

**顺带说明**：`week01/contacts.json`（数据文件）不进版本库，这已经做过了 —— 所以删代码文件不会碰到你的数据。

### 第 4 件：归档 W3 参考答案（**已由总指挥完成，跳过**）

`week01/chat_cli_reference.py` 已移到 `week03/reference/chat_cli_reference.py`，2 个 ruff 错也修好了（import 排序 + `datetime.now(tz=UTC)`）。**你不用做，只需要 `git log --oneline -1` 看一眼确认。**

### 第 5 件：`storage.py` 加结构校验（40 分钟，**今天最重要的一件**）

**背景（外部审计实跑发现的真 bug）**：`contacts.json` 若是合法 JSON 但结构不对，程序会崩。总指挥实测：

```
null        -> NoneType  | CRASH: 'NoneType' object has no attribute 'append'
{}          -> dict      | CRASH: 'dict' object has no attribute 'append'
"abc"       -> str       | CRASH: 'str' object has no attribute 'append'
123         -> int       | CRASH: 'int' object has no attribute 'append'
[[1,2]]     -> list      | 不崩，但列表里混进了 list（脏数据）
[{"name":1}] -> list     | 不崩，但 name 是 int（脏数据）
```

**根因一句话**：`json.loads` 只保证「能解析成 JSON」，不保证「是 `list[dict[str, str]]`」。**类型注解是给人看的，运行时不管。**

**这一格在大纲哪里**：W2「输入校验 + 分层」。**以后用在哪**：RAG 阶段读向量库/索引文件同理 —— 存档结构坏了要当场发现，不能让脏数据流进检索。

**要做两件事**：

1. `storage.py` 的 `load_contacts` 里，读完之后加一次结构检查：
   - 不是 `list` → 抛异常（**复用现有的 `json.JSONDecodeError`**，或自定义一个异常，你选）
   - 是 `list`，但里面有元素不是 `dict`、或缺 `name`/`phone` → 也抛
   - **注意**：校验失败**绝不能返回空列表**（返回 `[]` 后用户一敲 `quit`，`save_contacts` 就把 `[]` 写回去，真实数据被静默覆盖 —— 这个道理你 09-18 已经答对过）

2. 新建 `week01/contacts/test_contacts.py`（**10-15 行，用原生 `assert`，不装 pytest**），覆盖：
   - `add_contact` 加一条 → `len` 变 1
   - `find_contacts` 按「包含」命中 + 未命中返回 `[]`
   - `delete_contact` 正常删 / 越界返回 `None` / 序号 0 和负数返回 `None`
   - `load_contacts` 遇到 `null`、`{}`、`"abc"` 三种畸形文件 → 抛异常（不是返回 `[]`）

   运行方式：`uv run python week01/contacts/test_contacts.py`，全过就打印一行 `ALL TESTS PASSED`。

**验收（实跑给我看输出）**：
- `uv run python week01/contacts/test_contacts.py` → 打印通过
- 手动把 `contacts.json` 改成 `null` 再跑 `cli.py` → 提示「数据文件损坏」，**且 `contacts.json` 内容没被覆盖**
- 讲回：`json.loads` 和 `isinstance` 各自负责什么？为什么校验要放在 `storage.py` 而不是 `cli.py`？

**加分（时间够再做）**：给 `find` 加一个「按电话后 4 位查」的小变体 —— 这是「隔天独立改小需求」的检验，别现在做，**明天做**。

## 本轮结束条件

五条全过 + 能回答（讲回，不许看代码）：

1. `ruff` 的 I001 规则要你改哪两处？为什么 `DATA_FILE` 要排在 `load_contacts` 前面？
2. `if text == None` 和 `if not text` 有什么区别？哪个能判出 `[]`？
3. `json.loads` 和 `isinstance` 各自负责什么？为什么结构校验放在 `storage.py` 而不是 `cli.py`？
4. 为什么校验失败时**不能**返回空列表？（一句话，说清后果）

**节奏提醒**：本轮约 1.5 小时（ruff 15 分 + 错题本 30 分 + 删文件 5 分 + 结构校验与测试 40 分），按 28h/周 走；做完就停，不要预支 W3。

=== 复制结束 ===
