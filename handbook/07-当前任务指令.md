# 当前任务指令（总指挥下达 · 2026-09-17 13:45）

> 用法：子教练按 `.hermes.md` 启动协议自动读到本文件。总指挥每轮重写，子教练只执行不修改。

=== 复制开始 ===
【总指挥验收结果 · 阶段① W1】功能项**全部通过**（(总指挥实跑核过 `find 李` / `find 王` / `del abc` / `del 99` / `del 1` / 重开进程存盘）。等学员现场答完 3 道概念题，W1 正式收口、进入 W2。

## 本轮任务：把 `contacts.py` 拆成三个文件（W2 第一格）

**这一格在大纲哪里**：`ROADMAP.md` W2「函数、模块分层、类型注解、`pathlib`」。**以后用在哪**：RAG 项目也是同样的三层结构（数据层 / 业务层 / 接口层），面试必问「你的项目怎么组织的、某处改动会碰到几个文件」。另外昨天你自评「`pathlib` 只是了解了没学会」——本轮在 `storage.py` 里补上。

**要做的（按顺序，每完成一步就 commit）**

1. 新建 `week01/contacts/` 目录，把现在的 `contacts.py` 拆成三个文件：

| 文件 | 只负责 | **禁止出现** |
|---|---|---|
| `storage.py` | 读 / 写 `contacts.json`（用 `pathlib.Path`） | 任何 `print`、任何 `input` |
| `core.py` | 增 / 删 / 查的业务逻辑 | 任何 `print`、任何 `input` |
| `cli.py` | 接收输入、打印输出、`while` 主循环 | 任何文件读写 |

2. **所有函数加类型注解**（形如 `def load_contacts(path: Path) -> list[dict[str, str]]:`）。
3. 运行方式写进 README：`uv run python week01/contacts/cli.py`（直接跑脚本时该目录自动在 import 路径上，所以 `from storage import ...` 能用；不要用 `-m`，那是下一格再说）。
4. **行为不许变**：昨天 5 条命令（`add` / `list` / `find` / `del` / `quit`）的输出格式和存盘行为必须和现在一模一样 —— 重构的铁律是「对外行为不变」。
5. 顺手做一件工程卫生：数据文件不该进版本库。
   ```
   git rm --cached week01/contacts.json
   ```
   然后 `.gitignore` 加一行 `week01/contacts.json`，再建一个 `week01/contacts.example.json`（放 1 条假数据）提交。**以后 RAG 项目里的向量库/索引文件同理，绝不能提交。**

**验收（实跑给我看输出）**
- `uv run python week01/contacts/cli.py` 跑起来，5 条命令行为照旧
- `grep -n "print\|input" week01/contacts/storage.py week01/contacts/core.py` 无输出（分层干净）
- 每个函数都有类型注解（`uv run python -c "import typing"` 不算数，用眼睛看 + 讲回）

**讲回（必须做，讲不出来不往下走）**
1. 三个文件各自的职责边界，举一个「改这个功能只动哪个文件」的例子
2. `storage.py` 里为什么禁止 `print`？
3. 如果以后要把 json 换成数据库，你要改哪几个文件、为什么好处在这里

**本轮结束条件**：上面三项全过 + 能回答：①改 `find` 的匹配规则要动哪个文件 ②把存储换成 sqlite 要动哪个文件。
=== 复制结束 ===
