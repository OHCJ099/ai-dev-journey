# 当前任务指令（总指挥下达 · 2026-09-19 17:05）

> 用法：子教练按 `.hermes.md` 启动协议自动读到本文件。总指挥每轮重写，子教练只执行不修改。
> **规则（handbook/06）**：每次验收通过后必须重写本文件，否则「存在就以它为准」会一直派已完成的任务。

=== 复制开始 ===

【验收结果 · W2 收尾】**功能全过**（`ruff` 全仓通过、`test_contacts.py` 9 条 assert 全过、EOF 退出存盘、7 种畸形 json 全部拒绝且原文件未被覆盖）。**W2 还剩两个尾巴**，本轮收掉后进 W3。

## 本轮任务：W2 收口（约 1 小时）

**这在大纲哪里**：`ROADMAP.md` W2「函数、模块分层、类型注解、`pathlib`、**`.env`**、**`httpx`**」—— 后两项还没做。**以后用在哪**：`.env` 管密钥（W3 调 DeepSeek 的 key 就放这），`httpx` 是理解 HTTP 请求的底层工具（W6 上 FastAPI 时同一个东西的另一端）。

### 第 1 件：修 `week01/contacts/README.md`（20 分钟）

现在它落后于代码（还写「`quit` 时存盘」）。补齐五项：
1. 运行方式：`uv run python week01/contacts/cli.py`
2. 命令表（`add` / `list` / `find` / `del` / `quit`）
3. **意外退出（输入结束 / `EOFError`）也会存盘**，不是只有 `quit`
4. 数据文件结构不对时提示「数据文件损坏，请检查 contacts.json」，**不会覆盖原文件**
5. 测试命令：`uv run python week01/contacts/test_contacts.py`

**顺带**：测试里 `assert(...)` 带括号的写法（`assert` 是语句不是函数）改成 `assert x == y`。**全角/半角标点**是 W1 起的老毛病，写完扫一眼。

### 第 2 件：`.env` + `httpx` 最小练习（40 分钟）

**新建 `week02/` 目录**，做两件小事：

**2a. 读 `.env`（`.env` 已经存在，不用你建）**
- 仓库根的 `.env` 里已有 `DEEPSEEK_API_KEY`（总指挥已从 `D:\Hermes\.env` 复制过来，`.gitignore` 已忽略它）
- 写 `week02/read_env.py`：用 `python-dotenv` 的 `load_dotenv()` + `os.getenv()` 读出它，**只打印「读到没有」和「长度」，不要打印 key 本身** —— key 永远不进日志、不进截图、不进对话，这是习惯
- **验收**：`uv run python week02/read_env.py` 能打印出「读到 key」；`git status` 里**看不到 `.env`**
- 讲回：为什么 key 不能写进代码？如果已经推到 GitHub 了该怎么办？

**2b. `httpx` 发一次请求**
- 写 `week02/first_request.py`：用 `httpx.get()` 请求 `https://api.github.com/repos/OHCJ099/ai-dev-journey`，打印 `status_code` 和响应里的 `full_name` 字段
- **验收**：能打印出 `OHCJ099/ai-dev-journey`
- 讲回：`status_code` 200 和 404 分别代表什么？

> **提示**：`httpx.get(url)` 返回的响应对象，取 JSON 用 `.json()`（返回 dict），取状态码用 `.status_code`。这就是浏览器干的事，只是没有界面。

### 讲回题（口头答，子教练整理进日报）

1. 为什么 key 要放 `.env` 而不是写进代码？如果已经推到 GitHub 了该怎么办？
2. `httpx.get()` 和你在浏览器里输网址，本质区别是什么？
3. **（W2 分层考点，上轮没答）** `core.list_contacts` 返回 `list[tuple[int, dict]]`，那个 `int` 是「给人看的序号」。如果以后前端要**分页**，第 2 页第 1 条该显示 `1` 还是 `6`？这个决定该由 `core` 做还是 `cli` 做？

### 收工

**你只写 3 行**「今天学会了什么」。台账行与日报由子教练代笔。

**节奏**：约 1 小时。**W2 到此收口**，下一轮进 W3（DeepSeek API 首调）—— 那是本项目第一个真正的 AI 代码。

---

### 给子教练的 W3 预告（**重要，别做错**）

学员**天天在用各类 AI 工具**，对「token / 上下文窗口 / 流式输出 / 模型选择」有**用户视角的直觉**。所以 W3 的讲回重心**不是概念扫盲**，而是：

- **自己写代码实现**：怎么解析 SSE 流（`data: ` 前缀、`[DONE]` 结束标记）、怎么按错误码分类处理（401 / 402 / 429 / 超时）、怎么用 `usage` 字段算钱、上下文超长时怎么裁剪
- **不要**花时间解释「什么是 API key」「什么是大模型」—— 他知道。**直接上代码。**
- 验收标准不变：写出来 + 讲清为什么这么写。

=== 复制结束 ===
