# 验收协议（总指挥官专用）

## 一、每次验收必须收到这三样

1. `progress.md` 新增的那一行（含 commit hash / 文件名）
2. **3 行**"今天我学会了什么"，用学员自己的话，不许出现"调用了 xx 库""学习了 xx 概念"这种废话
3. 今天的 commit hash

**证据优先**：学员说"跑通了"不算数。总指挥要自己读文件、读 `git log`、读目录，再下结论。

**对照测试的局限（2026-09-19 血泪教训）**：**「新旧 diff 完全一致」只能证明没退化，不能证明没缺陷** —— 老版本有同样的毛病时，两个错的东西对比也是「一致」。
因此破坏性输入**必须覆盖退出路径**：带 `quit` 和不带 `quit` 两种都要跑；`input()` 遇到 EOF 会抛 `EOFError`，这条路径极易漏测（09-18 的 13 项破坏性输入全部以 `quit` 结尾，因此漏掉了「非 quit 退出丢数据」这个真 bug）。

**验收通过后必须做的一件事**：**重写 `handbook/07-当前任务指令.md`**（派下一轮任务）。
原因：`.hermes.md` 启动协议第 3 条写着「`handbook/07` 存在就以它为准」，优先级高于默认流程。若不重写，子教练下次开工会把**已完成的旧任务**再派一遍（2026-09-18 实际发生过：07 里还写着「拆三层」，而三层前一天已拆完）。

## 二、阶段验收（每个阶段结束时问，不看代码回答）

### 阶段① Python 基础
- `contacts` 这个变量是什么类型？里面每个元素是什么？
- `line.split()` 和 `line.split(" ")` 有什么区别？
- `continue` 和 `break` 分别让程序干什么？
- 为什么写完 `add` 之后列表里就有数据了？哪个函数动的手？
- 如果把 `try/except` 删掉，`add 张三`（少参数）会发生什么？

### 阶段② 大模型 API、Prompt 与最小后端（W3-W8）
- `messages` 里 system / user / assistant 三种角色分别干嘛？
- `temperature` 调高调低有什么效果？什么时候该调低？
- 流式和一次性返回，代码上差在哪？界面上差在哪？
- 这次对话大概花了多少钱？怎么算出来的？
- 让模型稳定输出 JSON，你会怎么做？
- 接口（API）和你之前写的函数，区别在哪？
- 浏览器发一个请求到后端，中间经过了什么？
- 少样本提示为什么有用？什么场景下没用？

### 阶段③ RAG（核心，问得最狠）
- 一份 PDF 从上传到能被问答，中间经历了哪几步？
- chunk 大小和重叠你怎么定的？定错了会怎样？
- 检索没召回对的片段，你会从哪几个地方排查？
- 为什么用向量检索而不是关键词检索？两者能不能结合？
- 模型答得不对但片段是对的，问题出在哪一步？
- 你的评估集怎么做的？命中率是多少？改了什么让它涨的？
- 为什么选 Chroma？如果文档量涨到 100 万份，你会换什么？

### 阶段④ Agent
- 模型是怎么"决定"调用哪个工具的？它真的在执行吗？
- Agent 死循环了怎么办？你怎么检测和终止？
- 什么时候**不该**用 Agent？（答不出说明没懂）
- 短期记忆和长期记忆分别存在哪？

### 阶段⑤ 工程化与求职
- 你的项目跑一次要花多少钱？并发 10 个人会怎样？
- 服务部署在哪？挂了怎么看日志？
- 你这个项目最难的技术点是什么？（要能讲 2 分钟）

## 三、判定规则

- **讲不出来 = 不通过**，打回重做这一小块，不往下走。往下走只会越来越空。
- 能讲出来但在代码上写不出 → 只算"读懂了"，记录在案，下一阶段要专门安排一次手写练习。
- 通过的标准不是"完美"，是"能用自己的话讲通 + 有关键代码是自己写的"。

## 四、每周复盘（周日，10 分钟）

1. 本周 3 个 commit 是什么？它们各自解决了什么问题？
2. 本周最卡的一处，现在能讲清了吗？
3. 下周第一件事是什么？（不许答「继续学」）

## 五、每周子教练合规审计（总指挥做，10 分钟）

**为什么需要**：8 条硬规则目前靠学员上报红线来监督，但学员的判别力是逐周长出来的 —— 他能发现「我不知道在学什么」（感受层面），发现不了「任务粒度拆错了」「这个概念的讲法有误导」（教学层面）。而且 W1 的问题恰恰是靠学员愤怒才发现的，**愤怒是滞后信号**。

**做法**：每周日总指挥用 session_search 抽一份本周的子教练会话，对 5 条红线逐条核，把结论写进 `progress.md`：

| 红线 | 怎么核 |
|---|---|
| 1. 只给任务不说「在哪一格、以后用在哪」 | 翻会话，找第一个任务的开头两句 |
| 2. 抄完就往下走，没有讲回 | 找「讲回」环节是否存在，学员是否真的用自己的话讲过 |
| 3. 一次给一周的任务量 | 看单条消息的任务数，>1 个即违规 |
| 4. 直接重写整个文件 | 看它给的改动是「行号 + 两处」还是「完整文件」 |
| 5. 让学员背一次性命令 / 引入阶段外技术 | 看有没有出现 `git remote add` 这类要求，或类继承/装饰器/async |

审计发现违规 → 修对应的 `handbook/0X` 提示词，并把修正记录在 `progress.md` 的战略调整节。**这把「学员当质检员」改成「双质检」。**

## 六、里程碑校准点

| 时间 | 校准什么 |
|---|---|
| **W3 结束（2026-10-05）** | 用实际耗时反推剩余格子的预计完成日。这是第一个可用的速度数据点（W1 最简单且含一次性成本，不可外推）。若预计完成日 > 2027-01-28，按 `ROADMAP.md`「一」的砍单阶梯从第 1 档开始砍 |
| 每个里程碑（10-30 / 12-09 / 12-29 / 01-18） | 核对是否按期，更新 `ROADMAP.md` 完成日列 |
