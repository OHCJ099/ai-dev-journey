# 子教练日报 · 2026-09-19

> 只写证据，不写「总指挥验收通过」这类结论（验收由总指挥下达）。

## 1. 今日产出

- 文件：
  - `week01/contacts/cli.py` —— import 分组排序（标准库 / 本项目，组内字母序、常量优先）；顶层函数前空两行；`except json.JSONDecodeError` → `except ValueError`（一个 `except` 兜住「json 语法坏」与「结构不对」）；删掉已无用的 `import json`；新增 `except EOFError` 分支（存盘 + 友好提示 + `return`）
  - `week01/contacts/storage.py` —— `load_contacts` 拆出 `data = json.loads(text)`，加三层结构校验：① 非 `list` → `raise ValueError`；② 元素非 `dict` → `raise ValueError`；③ 缺 `name` / `phone` → `raise ValueError`。两处 `isinstance` 后的 `raise` 加 `# noqa: TRY004` + 理由（**校验失败绝不 `return []`**）
  - `week01/contacts/core.py` —— 文件职责注释 + 三处「为什么」注释（`start=1` 是给用户看的序号、`in` 是子串匹配、`user_no` 是用户号非索引）
  - `week01/contacts/test_contacts.py`（新建）—— 9 条原生 `assert`：add 加一条 → `len` 变 1 / find 命中 / find 未命中返回 `[]` / delete 正常删 + `len` 减 1 / 删最后一个合法序号必须成功 / 越界 `6`、`0`、`-1` → `None`
  - 删除：`week01/contacts.py`（旧单文件，`git rm`，同名撞车隐患消除）
- commit：`79b2bd7`（lint）→ `0eba0dd`（删旧文件）→ `5467296`（EOF 存盘）→ `47d6354`（结构校验 + cli 改接 `ValueError`）→ `d0185c2`（三文件注释）→ `686a7f9`（core 注释精简）→ `fb22706`（测试）
- 是否已 push：**已 push**（`git log origin/main..HEAD --oneline` 输出 0 行）
- 我（子教练）自己核对的方式（读了哪些文件 / 跑了哪些命令）：
  - `read_file` 逐行读 `cli.py` / `storage.py` / `core.py` / `test_contacts.py` 全文
  - `uvx ruff check .` → `All checks passed!`
  - `uv run python week01/contacts/test_contacts.py` → `ALL TESTS PASSED`
  - **结构校验**：在系统临时目录的副本上直接调 `load_contacts` 跑 11 个用例（`{}` / `"abc"` / `123` / `null` / `[[1,2]]` / `["abc"]` / `[123]` / `[{"name":"甲"}]` / `[{"phone":"1"}]` / 合法一条 / `[]`），逐条打印异常消息 —— 确认三个 `raise` 各自命中，且 ② 在 ③ 之前（`[123]` 未被 `TypeError` 截胡）
  - **EOF 存盘**：副本上分别跑「不带 `quit`」与「正常 `quit`」两种退出，`grep -c '"phone"'` 记录数 7 → 9，两笔新增均落盘
  - `git show --stat` 核对每笔提交的文件清单；`git status --short` 确认工作区干净
  - 所有临时副本建在 `$LOCALAPPDATA/Temp` 下，跑完 `rm -rf`，仓库与 `week01/contacts.json`（7 条真数据）未受影响

**注**：本日中段总指挥重写了 `handbook/07-当前任务指令.md`（4 件收敛为 3 件）并修改 `notes/README.md`（笔记改由子教练代笔、学员每日书面输出限 3 行），子教练按新 07 执行。

## 2. 讲回记录

| 讲了哪几行 | 结果 | 错在哪 |
|---|---|---|
| cli 与 storage 的异常分工（谁抛 / 谁接 / 为什么） | 通过 | — 原话「如果异常给 storage 职责就坏了，它自己处理了，控制台自己打出异常，cli 想处理都处理不了」 |
| 「只能用 `raise`，不能用 `return`」的理由 | 部分通过 | 结论对，**理由错**：说成「`return` 不符合 `-> list[dict[str, str]]` 注解」。纠正两点：① `return []` 类型上合法；② **类型注解运行时完全不检查**（实测 `return "xxx"` 不报错，到调用方 `.append` 才炸）。真正理由是 `return` 只能送「正常结果」，表达不了失败；且错形状的值会把炸点推到离病根很远的地方 |
| `in` 在字典 vs 在字符串的含义 | 通过 | — |
| 注释「为什么」vs「做什么」 | 通过 | 首轮 4 条全写成「做什么」（`# add函数` 之类），指出后自行改为「为什么」 |
| 管道输入为什么会 `EOFError` | 通过 | 首轮说「程序还在等」，实际是 `input()` 一读就发现没人、**立刻抛错**（瞬间崩，不是干等） |
| 三个提示符的区别（`PS>` / `>>>` / 程序自己的 `>`） | 通过（补） | 误把 PowerShell 块粘进 Python REPL |

**3 行「今天我学会了什么」（学员原话，子教练仅修错别字与事实性笔误）**：

1. 我能说清为什么管道输入会崩、数据为什么会丢：手敲时是「你一步我一步」，敲 `quit` 才存盘；管道一次性把话说完，没有 `quit`，`input()` 读不到东西就抛 `EOFError`，`save_contacts` 那一步根本没走到。所以我能用 `try` / `except` 把这种情况接住，及时存盘。
2. 我能分清职责：让 `cli` 只**接**异常不抛异常，让 `storage` 判断文件读得对不对、格式对不对、有没有缺东西。如果让 `storage` 自己处理异常就坏了 —— 它自己处理了、控制台自己打出异常，`cli` 想处理都处理不了；而且多个模块都调 `load` 时处理方式会不一致。所以要把决定权交给 `cli`。
3. 今天我能做到用 `raise` 抛异常、用 `isinstance` 判断类型，让 `storage.py` 读 json 时用三层检查（不是列表 / 元素不是字典 / 缺键）抛异常交给 `cli` 处理，并且能活用 `in`（字符串里匹配子串，字典里匹配键）。

**踩的坑**：`"111"`（字符串）和 `111`（数字）看起来一样，实际不相等 —— 测试断言里写成数字 `111` 就失败。基础但易犯。

## 3. 卡点

- 已修：
  - `except EOFError` 漏 `return` → `while True` **死循环**（每圈都打印一遍名单，因为 `line` 还是上一圈的 `"list"` 没被覆盖）；补 `return` 后正常
  - `assert(delete_contact(contacts, 1)) == ({"name": "甲1", "phone": 111})` → `AssertionError`：`"111"`(str) ≠ `111`(int)
  - `999` 当越界用例 —— 与不合法值结果相同，**测不出 off-by-one**；改为 `5`（最后合法，必须删成功，且断言删的是 `乙3`）+ `6`（越界）
  - `test_contacts.py` 首版 import 顺序不合 `I001` —— 由学员自己发现并修正
  - `cli.py` 改 `except ValueError` 后 `import json` 变为未使用（`F401`）→ 删掉该行
  - 注释里把 `c`（字典）写成列表；`start=1` 那条缩短后语义丢失，两轮才改回
  - PowerShell 块粘进 Python REPL（`>>>`）导致整段 `SyntaxError`（一行未执行、无副作用）
  - `week01/contacts_bak.json`（学员自建备份）已删除，未进版本库
- 未修（留给下一轮）：
  - `week01/contacts/README.md` 已落后于代码：仍写「quit 时存盘」（现在意外退出也存盘）、未提结构校验、未提测试命令
  - 测试文件里 `assert(...)` 带括号的写法仍在（`assert` 不是函数，能跑但误导）
  - 全角 / 半角标点仍是手滑高发区（W1 起的老毛病）
  - **`handbook/07-当前任务指令.md` 已过期**（它还写着「剩 2 件」，两件都已完成）—— 按协议 07 优先级最高，若不重写，下一次子教练会话会重派已完成的活。重写属总指挥职责，子教练不动

## 4. 明天第一件事（只许一条）

更新 `week01/contacts/README.md`，让它和当前代码一致：① 运行方式；② 命令表；③ **意外退出（`EOFError`）也会存盘**，不是只有 `quit`；④ 数据文件结构不对时会提示「数据文件损坏，请检查 contacts.json」而不是崩；⑤ 测试命令 `uv run python week01/contacts/test_contacts.py`。这是 W2 的产物，不算预支 W3。
