# 子教练日报 · 2026-09-17

## 1. 今日产出
- 文件：
  - `week01/contacts.py` —— `find` 改为 `in` 包含匹配并打印姓名+电话；`list`/`find` 输出带序号（`enumerate(..., start=1)`）；新增 `del` 分支（`ValueError` 兜底 + 链式范围校验 + `pop` 返回被删记录并打印）
  - `notes/README.md` —— 笔记规范（错题本 / 概念卡 / 实验记录三类 + 第三方教程按需检索映射）
  - `.hermes.md`、`ROADMAP.md` —— 补笔记规范指引与教程章节映射
- commit：`5c2399d`（find 模糊匹配）、`b431d0c`（序号）、`5d3be86`（del 分支）、`7c9add4`（except 补 `continue`）
- 是否已 push：是（`git log origin/main..HEAD` 为空，工作区干净）
- 我（子教练）自己核对的方式：
  - `read_file week01/contacts.py`（全 69 行，逐行读）
  - `printf 'list\ndel abc\ndel 99\ndel 1\nlist\nquit\n' | uv run python week01/contacts.py` 实跑
  - 重开进程再 `list`，确认删除已存盘（`contacts.json` 同步核对）
  - `git log --oneline` / `git status --porcelain` / `git diff` 确认最后一次改动已提交

## 2. 讲回记录
| 讲了哪几行 | 结果 | 错在哪 |
|---|---|---|
| `find` 分支第 39-40 行（`in` vs `==`、打印的是谁） | 通过 | 未点出 `name` 是用户输入、`contact["name"]` 是数据（子教练补充，30 秒实验演示） |
| `enumerate` 为什么用 `start=1` | 通过 | 第一遍表述为「del 要 +1」，方向反了；第二遍「用户看到的号和代码用的号必须是同一个号」讲对 |
| `try` 该包住哪些行 | 通过 | 第一版把 `if/else` 全包进 `try`（异常范围过大），已收窄到只包 `int()` |
| 链式比较 `len(contacts) >= del_index >= 1` | 通过 | — |
| 下标 −1 换算 | 第二次通过 | 第一遍「输入 2 对应索引中第三个」错一格；第二遍「2−1=1，删下标 1 即用户看到的 2 号」正确 |
| `pop` vs `remove`（参数 / 返回值） | 通过 | — |

## 3. 卡点
- 已修：`\n` 与 `print` 自带换行重复（输出多空行）；全角冒号 `：`；`try` 范围过大；`except` 里缺 `continue`
- 未修（留给下一轮）：无 —— W1 三项返修（find 包含匹配 / find 打印电话 / 序号 + del）全部结清
- 流程问题（记录在案，非代码问题）：
  1. 改了没保存 1 次（`\n` 那次，磁盘上还是旧内容）
  2. 存了、跑了、忘了提交 1 次 —— `5d3be86` 推上去的 `del` 分支缺 `continue`（`except` 后直接往下走会 `NameError`），本地后来修好但未提交；已由 `7c9add4` 补上并推送

## 4. 明天第一件事
把 `week01/contacts.py` 拆成 `week01/contacts/` 三个文件（`storage.py` 只管读写 json / `core.py` 只管增删查逻辑 / `cli.py` 只管输入输出），所有函数加类型注解；`pathlib` 在 `storage.py` 里补上。
