# 子教练日报 · 2026-09-23

> 只写证据，不写「总指挥验收通过」这类结论（验收由总指挥下达）。

## 1. 今日产出

- 文件：
  - `week04/chat_cli/main.py` —— ① **历史裁剪**：`MAX_TURNS = 10`、`keep = MAX_TURNS * 2`、`if len(history) > keep + 1: history = [history[0]] + history[-keep:]`，**放在 `append_reply` 之后**（学员从终端里出现的 `10.5` 自己反推出：放在 `ask` 之前时历史是奇数条，`[-20:]` 会切掉最老的 user、留下配对的 assistant）；② **`argparse`**：模块级 `args = parser.parse_args()` + `--system`，默认值只留 `default=` 一处，`/clear` 与开场共用 `args.system`；③ **日志**：`logger = logging.getLogger(__name__)`、`logging.getLogger("httpx2").setLevel(logging.WARNING)`、`line[:20]` / `reply[:20]`、`basicConfig(filename=Path(__file__).parent / "chat.log", level=INFO, format=..., encoding="utf-8")`
  - `week04/README.md`（4 段：运行 / 命令 / 功能 / 技术栈 + 踩的坑 3 条，均为「现象 + 原因 + 修法」）
  - `.gitignore` 加 `week04/chat_cli/chat.log`
  - `notes/gotchas.md` 记 2 条（日志字段名打错 → 静默丢日志；文件名大小写**第 2 次**）
- commit：`1101274`（三件功能 + README 初版）→ `212e311`（README 补全 + 文件名改小写）→ `c611b04`（README 修正 `/save` 说明与功能描述）
- 是否已 push：**已 push**（实测 `git rev-parse HEAD origin/main` 两者相同 = `c611b04`）
- 我（子教练）自己核对的方式（读了哪些文件 / 跑了哪些命令）：
  - 逐版读盘 `week04/chat_cli/main.py`、`week04/README.md`、`.gitignore`、`week04/log_demo.py`（学员多次改写，报位置前重读）
  - `uvx ruff check .` → `All checks passed!`；`uvx pyright --pythonpath .venv/Scripts/python.exe week04` → `0 errors, 0 warnings, 0 informations`（**不带 `--pythonpath` 会去解析 `week04/reference/` 的参考答案，报 6 个 `reportMissingImports` 假错 —— 已实测**）
  - 实跑：`--system` 不带值 → **退出码 2**、无 Traceback；默认角色启动正常；`--system "你是夫子"` → `/clear` → 问「你是谁」→「我是夫子」（角色保留）
  - 实测 `week04/chat_cli/session.json`：`len = 21`，roles = `system` + 10 组 user/assistant 交替（裁剪封顶生效、结构干净）
  - 用学员自己的 `main.py` 跑 2 轮 → 新日志**无** `HTTP Request` 行，`AI说` 尾部正好切到 20 字
  - 探针确证噪音来源：`format` 加 `%(name)s` 后实测那条日志的 logger 名是 **`httpx2`**（不是 `httpx`）；`logging.getLogger("httpx2").setLevel(WARNING)` 实测可压掉（`probe3.log` 为空）
  - 模拟脚本实测两种阈值（`> keep` 与 `> keep + 1`）：第 10/11/12 轮的发送条数与第一条 role **逐行相同**（`1 + 2k` 恒为奇数，`> 20` 命中 21 时切片结果不变）
  - `git cat-file -t` 校验三个 commit hash 均为 `commit` 对象
  - 在临时仓库实测 `git mv README.MD README.md` 在 Windows + Git Bash 下可用后才发给学员

## 2. 讲回记录

| 讲了哪几题 | 结果 | 错在哪 |
|---|---|---|
| 历史裁剪 4 题 | 3 过 1 判错（**判错是子教练的**） | ① 「漏 `+1` 的后果」：学员答「容量 20 条、第一条仍是 system」——在**切片保留量**口径下正确；子教练出题时没写清口径（保留量 / 发送时刻 / 落盘三义），且子教练原预期的「assistant 打头」经实测不成立。② 裁剪位置 ✅（放 `ask` 之后，否则切掉 `u1` 留下 `a1`）。③ 时序 ✅（学员自我修正为「第 12 轮发送之前」，即第 11 轮收完回复后；发出时刻封顶 22 条）。④ 三个理由 ✅（成本线性涨 / 超上下文窗口 / 久远内容干扰） |
| `argparse` 3 题 | 通过 | `--system` vs `/system` ✅（「一个启动时定、一个中途改；前者只读一次」）；不带值行为 ✅（退出码 2）；默认值只留一处 ✅。选择「`/clear` 回到启动时角色」的理由：学员答「没有客户要求时自行选择」——子教练接受，但要求能说清两种行为差别（已能） |
| 日志 1 题 | 通过 | 日志 vs `print` ✅（「日志可追溯、写磁盘；print 只是打印」）；`basicConfig` 重复调用 ✅（学员实测「调了也没反应」——机制正确：root 已有 handler 时 no-op） |
| 坑 3 条表述校准 | 通过 | 列表拼接（`[history[0]]` 的方括号不能少）、日志字段名写错、裁剪时序 —— 三条都写进了 README |

**3 行「今天我学会了什么」**（学员原话）：

1. 「历史裁剪：将 history 的 system prompt 和最新的 10 条对话进行拼接，重新赋值给 history，完成历史裁剪。」
2. 「能用 `argparse` 加 `--system` 做到 `uv run` 加上启动项启动就能指定角色；`ArgumentParser` 是建一个参数解析器对象、`add_argument` 声明一个参数、`default` 是默认参数（没有命令时给什么）、`parse_args` 是读命令行装进 `args`、`args.system` 是取值把值取出来用。」
3. 「学会了日志，每轮往 `chat.log` 追加行；`print` 是说给人看的，`logging` 日志是记下来事后查的，两者用途不一样，各司其职。」

## 3. 卡点

- 已修：
  - 历史裁剪位置：先写在 `ask` 之前 → 终端里出现 `10.5`，学员自己推出「奇数条切片会切掉最老的 user」→ 移到 `append_reply` 之后
  - `SYSTEM_PROMPT = parser.parse_args()` 变量名与内容不符（装的是 Namespace）→ 改名 `args`
  - `try/except SystemExit` 包住 `parse_args()` → `--system` 不带值时 `NameError`（实测 Traceback，退出码 1）→ 删掉这层包装，交给 argparse 自己退出（退出码 2）
  - `/clear` 与 `--system` 来源不一致（中间版写死了一个常量 → `/clear` 后角色变回默认）→ 统一用 `args.system`，默认值只留 `default=` 一处
  - 日志文件 0 字节：`format` 里 `asctimeds` / `levelnamed` 拼错 → logging **静默丢日志**（只往 stderr 打 `--- Logging error ---`）→ 改回 `asctime` / `levelname`
  - 日志噪音 `HTTP Request`：`setLevel("httpx")` 无效 → 探针实测 logger 名为 `httpx2` → 改名后压掉
  - 日志未取前 20 字 → 补 `line[:20]` / `reply[:20]`
  - `ruff LOG015`（root-logger-call）×2 + `I001` → 改用 `logger = logging.getLogger(__name__)`；`ruff check --fix` + `ruff format week04` 收尾
  - README 文件名 `README.MD` → `git mv` 改小写；README 里 `/save` 写成 `history.json` → 改 `session.json`；README 里残留 1 处模板占位符与子教练的提示语 → 已清
- 未修（留给下一轮 / 请总指挥处理）：
  - **`handbook/07` 销项（请总指挥核销）**：本轮 07 的「第 1/2/3 件 + 收口 1-3 条」全部完成 —— 证据 `1101274` / `212e311` / `c611b04`（均已 push）、`ruff` 全绿、`pyright` 0 errors、`session.json` 21 条封顶实测。按 `.hermes.md` 硬规则 10，子教练不改 07
  - **`ROADMAP` 天数需同步**：「六、当前坐标」已更新为 131 天（2026-09-23），但「一、总目标」处的天数仍是 132 —— 子教练无权改该段，请总指挥同步
  - **子教练本轮两处缺陷**：① 给 `logging` 样例时用 `logging.info`（root logger）却没先跑一遍 `ruff`，导致学员项目里报 `LOG015` —— 与 09-22 流式那次同类（硬规则 9「先讲后练」的同类变体：**给样例前要先在学员的真实项目里跑一遍**）；② 断言「那条噪音来自 `httpx`」，实测是 `httpx2`，名字给错
  - **教学颗粒度**：本件子教练一次抛出 `getLogger` / `setLevel` / `propagate` / LogRecord 字段 / `basicConfig` 六个参数，学员明确反馈「讲的什么鬼东西，看不懂，东西很多参数很多，多出来莫名其妙这么多东西我不想看」→ 已回退为「照抄 4 处 + 每处理由一句」。**教训：40 分钟的小件只讲够用的那一层**
  - `week02/` `week03/` 仍无 README（低优先，风格不齐）
  - `chat_cli` 的 Ctrl+C 仍打 `KeyboardInterrupt` Traceback（`week01/contacts/cli.py` 有 `try/finally`，本文件没有）→ 建议列入 W5 或加练
  - 学员自测时贴过一次**改动前**的输出（16:57 的日志）导致多查一轮 → 已提醒「贴证据要贴改完重跑的输出」

## 4. 明天第一件事（只许一条）

**W4 待总指挥验收**：内容 7 项 + README 已全部交付，三个 commit 已 push（`1101274` / `212e311` / `c611b04`）。

验收通过后进 **W5 第一件：`week05/prompt_lab.py`**（同一个问题用多种 prompt 跑对比：角色 / 少样本 / 结构化输出）—— `handbook/07` 由总指挥重写后再执行。
