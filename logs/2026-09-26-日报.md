# 子教练日报 · 2026-09-26

> 规则：只写证据，不写"总指挥验收通过"这类结论（验收由总指挥下达）。

## 1. 今日产出

- **文件**：
  - `week07/main.py`（94 行，新建）—— `/chat/stream` 流式接口（SSE）+ 静态挂载 + `ChatStreamRequest`（`messages` 数组 + `Field(min_length=1)`）
  - `week07/chat_web/static/index.html`（124 行）—— 单页聊天页（fetch 收流 + 拆帧 + 前端维护 history + 断流兜底 + 防连点）
  - `week07/README.md`（子教练代笔，4 段 + 坑 4 条）
  - `week07/html_demo.html`（20 行，HTML/JS 最小演示 —— 补课用，未进 README）
  - `week07/reference/index.html`（参考实现，卡 25 分钟可看）
- **commit**：`3a01125`（流式接口）、`2789b1c`（单页聊天页）、`f2b31e0`（W7 收口：messages 数组 + 断流兜底 + README）
- **是否已 push**：**全部已推**（20:05 网络恢复后补推成功：`2789b1c..4d1dc66 main -> main`，`git log origin/main..HEAD` 为空）。期间 3 次失败均为 `Connection was reset` / `Could not connect to github.com:443`（网络侧，非仓库问题）。
- **我（子教练）自己核对的方式**：
  - `read_file` 读 `week07/main.py`、`chat_web/static/index.html`、`README.md`（多轮，含改前改后）
  - **自建探针**（scratch，不碰学员仓库）：用真实 SSE 字节流（每 5 字节硬切，必然切碎中文）跑学员页面 JS 的 5 个场景 —— A 正常流 / B 后端挂 / C 422 / D 飞行中锁定+解锁 / E 空输入；另用 node 验证 `TextDecoder` 有无 `{stream:true}` 的差异（`��好` vs `你好`）
  - **真起 uvicorn 实测**（8130/8131 两个端口）：多轮历史（「我叫小王」→ 答「你叫小王」）、空 `messages` → 422、静态页 200、`/chat` 未受影响
  - `git diff` / `git log` / `git status` / 静态三件（`ruff check .` 全绿、`ruff format --check .` 37 files、`pyright week07` 0 errors）

- **学员自述（今天学会了什么，3 行）**（学员 19:24 情绪激动，未按格式给 3 行 —— 由子教练从其口头表述整理，**待学员确认**）：
  ① 会写流式接口了：SDK 分片 → `yield` → SSE 格式 → `StreamingResponse` 推给浏览器
  ② 前端 `fetch` 收流：`getReader` 抽纸口 + `TextDecoder` 翻译机 + 拆帧（半条/多条都要处理）
  ③ 无状态接口 + history 前端维护；断流兜底（catch 提示 + finally 解锁）

## 2. 讲回记录

| 讲了什么 | 结果 | 错在哪 / 补了什么 |
|---|---|---|
| W6 尾巴：`# type: ignore` 为什么需要 | 通过（补讲后） | 学员第一轮答「不懂、AI 加的」→ 补讲「SDK 严格形状 + 字面量 vs 变量」，学员复述「SDK 对 message 有预设关键字，变量给它伪装了一遍」→ 基本对（补：变量也抹掉了 role 信息） |
| `[DONE]` 是不是 SSE 标准 | 通过 | 学员答「不是」✅ |
| `gen()` 为什么不是 `async def` | 通过（半） | 学员答「不能异步吧」→ 补讲：同步生成器被 Starlette 放线程池（`iterate_in_threadpool`，子教练读源码证实），`async def` 里调同步 SDK 反而堵事件循环 |
| 流到一半上游断了怎么办 | **不通过 → 补讲** | 学员答「抛异常呗，之前做过兜底」—— 那套 try/except 只管「还没开始流」；一旦开始流，响应头 200 已发出，状态码改不了。正解：try/except 挪进 `gen()`，错误也变成 `data:` 消息发出 |
| SSE vs WebSocket | **不通过** | 学员答「SSE 是流式，后者不是」—— 错。正解：SSE 单向、普通 HTTP；WebSocket 双向、要额外协议。聊天是一问一答的单向流 → SSE 够用且更简单 |
| 接口为什么无状态、history 谁维护 | **不通过（半）** | 学员答「不知道……给前端维护」—— 维护方答对；「为什么」不会。正解：服务要同时接很多人，服务端记住每人的历史就要管存储/过期/清理；无状态让服务器只管「收 → 答」 |
| `sendBtn.disabled` + `finally` 解锁 | **通过** | 「解决反复点击重复发请求；解锁放 finally 因为最后一定要解锁，不然锁死再也发不出去」—— 对 ✅ |
| TextDecoder 干什么 | **不通过（半）** | 学员答「把字一个个接住拼起来」—— 方向对但不完整。正解：字节 → 中文；中文 3 字节，被切开时靠 `{stream:true}` 续上 |
| 前端收流 vs curl `-N` 收流 | **不通过（半）** | 学员答「可能和 stream 不匹配」—— 正解：本质是同一件事（curl = 命令行浏览器，`-N` 关缓冲），后端那一头完全一样 |

**合计：9 题，首轮通过 3.5 / 不通过 5.5。**

**当日 20:00 前补齐（学员要求「现在就搞清楚，别拖到明天」）**：5 道未过题由子教练逐题讲解（SSE vs WebSocket 判据 / 无状态为什么 / TextDecoder 两层防线 / curl 对比 / 流中断），其中**第 5 题学员完成实修**：`gen()` 内 `try/except` + 错误帧（`4d1dc66`，含 ruff 默认规则 `BLE001` 的带理由 `noqa`），并真机验证 —— 临时改坏 `MODEL` → 页面显示「[错误] 上游异常，请重试」✅，测后已改回 `deepseek-flash`。**①②③④ 为子教练直给答案 + 学员阅读，学员未复述 —— 如实记录，验收时请总指挥抽查。**

## 3. 卡点

**已修**

1. 静态页 404：`main.py` 没挂载 `StaticFiles` → 补 `app.mount`（子教练给的骨架里漏了这行，学员实测撞上）
2. curl 输出每条 `data: None`：`yield f"data: {None}\n\n"` 在 if/else 外、for 内 + `{None}` 字面量 → 删行；判空改 `if not chunk.choices:`
3. 前端 422：前端发 `{message:...}`、后端收 `messages` 数组 → 前端改键名 + 后端新模型 `ChatStreamRequest`
4. 后端挂时页面无反应：`fetch` 异常未接 + `replyText` 未声明在 try 外 → `try/catch/finally`（**catch 部分由 DeepSeek 顺手改，学员已讲回通过**）
5. 探针服务两次踩坑（子教练侧，已修）：`--app-dir` 在 git-bash 下模块名解析失败；探针跑在 scratch 目录时 `StaticFiles` 相对路径找不到 → 均改绝对路径

**未修（留给下一轮）**

- `f2b31e0` 未 push（网络）
- 讲回 5 题未过（SSE vs WebSocket / 无状态为什么 / TextDecoder / curl 对比 / 流中断）
- W7 加练未做：Ctrl+C Traceback（W4 遗留）、week02/week03 README 缺、页面「重新开始」按钮

**体系问题（请总指挥裁定 —— 子教练不自行改制度文件）**

1. **【最重要】第 2 件教学顺序错误（子教练认账）**：直接让学员在**未教过 HTML/JS** 的页面上做「空位填空」。学员原话：「我他妈根本就没学过 html，你这个 script 里面写的一堆什么玩意我根本就看不懂」「讲都没讲学都没学直接开始内容填空，fastapi 也是一样的狗屎问题」。
   - 违反 `.hermes.md` 硬规则 9（没学过的必须先讲再出题）—— 而且**同一违规当天重犯两次**（HTML 整页；`app.mount` 静态挂载也是没讲就要求改）
   - 学员自评今日效率「极低」：「一整天什么都没干就写了一个循环」「一大半时间死盯着不理解的代码」。**实际产出并不低**（3 个 commit、流式链路端到端跑通），**但学习体验极差**，这是真实的体系故障，不是学员情绪。
   - **根因**：W7 的「前端最小页」在排期里被当作「最小」格子，实际引入了 **HTML + JS + 浏览器流式 API 三套全新知识（约 15 个概念）**，且都不是 Python。W6 的颗粒度平移规则（框架样板直接给）没有覆盖「全新语言/生态」这一情况。
   - **同一压缩出现在三份文件**（学员 09-26 晚追问「为什么规划十几个全新知识点你却觉得很合理」后补记）：`ROADMAP.md:57` 只写 3 个名词（单 HTML + fetch 流式/SSE）；`07` 按 **1h20** 估时且把「JS 的 fetch + getReader」归为「样板」；`notes/概念地图.md` 整个 W7 只占一行「前端 fetch / SSE 流式接收」。**三份文件都没把「新语言」的成本显性化，所以没有任何一处会触发警报** —— 这解释了为什么子教练与总指挥都没拦住。
   - **建议**（供总指挥定夺）：① `.hermes.md` 或 `handbook/02` 补一条：**引入全新语言/生态（HTML/JS/Docker/前端框架）时，按「先讲 + 最小可抄样例」处理，禁止直接出填空题**；② 未来类似格子在 `07` 里必须写明「本格引入的新知识清单」及各自的讲解方式。
2. **子教练两次「读旧版本下结论」**：① 判「`sendBtn.disabled = true` 没加」时读的是改前文件（学员已让 DeepSeek 改过）；② catch 里的 `appendChild` 判断同样基于旧版。学员两次都是对的。**已按 `learning-coach` 的「stale read」教训自查，但同日重犯两次** —— 建议在 `handbook/02` 收口清单里加一条硬动作：**判学员「没做 X」之前，必须先 `grep X` 一次（拿当前文件），不许拿对话历史里的印象**。
3. **DeepSeek 顺手改代码**（学员 09-26 首次出现）：学员让 DeepSeek 给代码「加注释」，它顺手把 `if (!resp.ok)` 检查、`try/catch/finally` 结构都改了。**这次改得对**（子教练用探针验证：删掉 `resp.ok` 检查 + 422 → 页面会显示空 AI 气泡并写入空 assistant 历史），**且学员讲回通过**。但这是新模式：**外部 AI 改的代码，学员必须能讲回**（已按 `learning-coach` 规则处理，未质问学员）。
4. **今日用时**：15:19 开工 → 19:24（学员 19:24 报情绪），约 4h05，中间含子教练自测/探针时间。**第 3 件讲回未过 + 加练未做**，按「深度要求」判定：**功能层 ✅、讲回层 ❌、破坏性测试 ✅**。

## 4. 明天第一件事（只许一条）

**先补 W7 的 5 道讲回题**（用今天写的代码现场对照讲，不重敲）：SSE vs WebSocket / 无状态为什么 / TextDecoder / 前端收流 vs curl / 流中断怎么办。讲回过了 → `f2b31e0` 补推 → 报总指挥验收。
