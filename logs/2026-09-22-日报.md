# 子教练日报 · 2026-09-22

> 只写证据，不写「总指挥验收通过」这类结论（验收由总指挥下达）。

## 1. 今日产出

- 文件：
  - `week04/chat_cli/llm.py` —— ① `ask(history)` 改**流式**：`stream=True`、`for chunk in stream`、`.delta.content`、`print(piece, end="", flush=True)`；② 收尾块处理（**先取 `usage` 再 `if not chunk.choices: continue`**，循环前 `usage = 0` 兜底）；③ 错误分支改为 `msg = "..."` → `print(msg)` → `return msg, 0`（修「选 A 之后错误提示被静默吞掉，只剩 `[本轮 0 tokens]`」）；④ docstring 记明 v1 妥协（打印暂留在本文件，W6 改 SSE）→ `6957e9b`
  - `week04/chat_cli/main.py` —— ① 四个命令：`/clear`（`history = [{"role": "system", "content": SYSTEM_PROMPT}]`，保留 system）、`/system <角色>`（`parts = line.split(maxsplit=1)` + `history[0]["content"] = parts[1]`）、`/save`（`json.dumps(history, ensure_ascii=False, indent=2)` → `Path(__file__).parent / "session.json"`）、`/exit`；② 未知命令兜底（`/` 与 `/help` 不再花钱发给模型）；③ 空行/纯空格兜底；④ 删死代码（不可达的 `elif parts[0] == ''` / `else: 输入无效`）与调试脚手架、修入口 `if __name__ == "__main__":` 被缩进进 `main()` 导致的「跑起来什么都不做」→ `6957e9b`
  - `.gitignore` —— 加 `week04/chat_cli/session.json`（同 W2 的 `contacts.json` 处理）→ `6957e9b`
  - 子教练侧：`week04/reference/{llm,main}.py`（答案，实跑验证过）
- commit：`93840c4`（多轮上下文，学员 14:49 自提）→ `6957e9b`（流式 + 四个命令）
- 是否已 push：**已 push**（文档提交 `00479e4`）
- 我（子教练）自己核对的方式（读了哪些文件 / 跑了哪些命令）：
  - `read_file` 逐版读 `week04/chat_cli/{llm,main}.py`（学员当日多次改写，每次报行号前重新读盘）
  - `uvx ruff check .` → `All checks passed!`；`uvx ruff format --check week04` → 已格式化；`uvx pyright --pythonpath .venv/Scripts/python.exe week04` → **0 errors**（修前实测报 `llm.py:63 - error: "usage" is possibly unbound`）
  - 实跑 6 条破坏性测试（管道喂入，一次跑完）：空行/纯空格 → 「请勿输入空字符串！」不花钱；`/` 与 `/help` → 「未知命令」不花钱；正常消息 26/42 tokens 有回答；`/clear` 后问「我第一句说了什么」→ 模型只能答出 system 那句（「我叫小明」确实被清掉）；`/system` 不带参 → 正确提示；`/system 你是个   只会说文言文的夫子` → 下一句立刻文言文（多空格未被切碎）；`/save` → `week04/chat_cli/session.json` 中文正常、`role`/`content` 结构对
  - 单独探针确证流式返回结构：`0-3 号块 choices:1 / usage:None`（首块 `delta` 为空字符串、末块 `delta` 为 None），`4 号块 choices:0 + CompletionUsage(...)` —— 用于给学员补讲「为什么 `chunk.choices[0]` 会 `IndexError`」
  - 直接问 API 确证 400 文案：`messages=[{"system": ...}]` → `APIStatusError: 400 {'message': 'messages.0.role 取值无效'}`（用于给学员定位 `/clear` 的字典键错误）

## 2. 讲回记录

| 讲了哪几题 | 结果 | 错在哪 |
|---|---|---|
| 多轮上下文 4 题 | 通过（Q1 首答纠正后过） | Q1 首答「8 条」→ 正解 **9 条 = 轮数×2 + 1 条 system**，学员漏掉最上面那条 system；Q2「不发全历史就讲了下句忘上句」✅；Q3 `append` 原地加 vs `=` 换指向 ✅（学员自己的话「新造一个列表 vs 在原有列表上添加」）；Q4 空行浪费 token ✅ |
| 流式 2 题 | 概念全过，**代码未落实** | 「usage 只在最后一块」✅（原话「只有所有 chunk 输出完、句子结束了，usage 才有意义」）；「中转不返回 usage 时应返回 0」✅ —— **但代码里 `usage = 0` 没写，pyright 实测报 `possibly unbound`**。已就此事讲明「想对了 ≠ 写对了」 |
| 四个命令 3 题 | 2 过 1 不过 | `split(maxsplit=1)` ✅；`/system` 不带参 ✅（但只答「程序不能崩」这个原则，未答机制 = `parts[1]` 下标越界 `IndexError`）；**`/clear` 换指向为何安全 ❌** —— 学员答「`append_reply` 长期持有，键值对匹配上了」，正解：**没有任何函数长期持有那个列表**，`ask(history)` / `append_reply(history, reply)` 都是每次调用时现传参数，所以 `main` 换指向后下一轮传进去的就是新列表 |

**3 行「今天我学会了什么」**（学员原话，待补）：_

## 3. 卡点

- 已修：
  - 流式崩溃 `IndexError: list index out of range`（`chunk.choices[0]` 在收尾块上越界）→ 补讲返回结构 + `if not chunk.choices: continue`（且必须**先取 usage**）
  - `UnboundLocalError: usage`（`usage = 0` 漏写）→ 补回初值
  - `/clear` 后「AI > [本轮 0 tokens]」无回答 → 根因两处：① `history = [{"system": ...}]` 字典键错（API 实测 400 `messages.0.role 取值无效`）② 选 A 后错误提示无人打印（`except` 只 `return` 不 `print`）
  - `/system` 看似成功实则没改 → `history[0]["system"] = ...` 新增了垃圾键，应为 `["content"]`
  - 正常消息全走「输入无效」→ `if not parts[0]:` 条件写反（学员自查后已改）
  - 程序跑起来什么都不做 → `if __name__ == "__main__":` 被缩进进 `main()` 函数体内（缩进 = 归属，W1 的老坑换了个地方）
  - `session.json` 缺 `ensure_ascii` / 相对路径 → 改用 `Path(__file__).parent` + `ensure_ascii=False, indent=2`
- 未修（留给下一轮）：
  - **`07` 的措辞问题（请总指挥改）**：本轮 07 把「流式下 `usage` 什么时候才有」写成让学员「自己想」的题。**那是服务端返回结构，属于硬规则 9 的「陌生 API」，必须先讲再练** —— 学员为此卡了 40+ 分钟，原话「我压根就不知道机制…我没有办法我卡了好久」。子教练当场补讲后通过，但这一处**是派单文档的缺陷，不是学员的问题**（另：`07` 的「第 1 件/第 2 件」与本日报的件号口径需统一，避免下次会话再对不上）
  - **编辑器旧缓冲覆盖磁盘改动（当日两次）**：学员 VS Code 里是旧 buffer，保存时把子教练已改好的内容回滚（噪音注释、调试 print、`/save`、`/exit` 提示）→ 处理办法已告知学员：改文件前先 `Ctrl+Shift+P → Revert File`；子教练每次报行号前重新读盘
  - 子教练一次**行号过期**（学员在我读盘之后又改过文件）被当场指出「完全对不上」——**学员对**，已改为按内容指位
  - 学员代码里出现外部 AI 生成痕迹：`# 如果你后面要用到 json，就保留，否则删掉` 这类解释性注释、`/exit` 打印「删除成功」的占位提示 → 已清；按既定口径**不追问来源**，只按「他没学过/没写过」处理
  - `week02/` 仍无 README（低优先，风格不齐）

## 4. 明天第一件事（只许一条）

**W4 第 3 件 + 收口**：token 累计统计（循环外累计，`/exit` 与 `/save` 打印「本次会话共 N tokens」）→ 跑 `handbook/07` 的 W4 收口 6 条（**连聊 10 轮不走样**、四命令、token 统计、`ruff` 全绿、commit + push、写 `week04/README.md` 四段）。

**排期提醒**：学员 09-23 晚回家 + 收拾电脑，时间可能被占 → **收口优先，不加练**（07 文末的三条加练不动）。
