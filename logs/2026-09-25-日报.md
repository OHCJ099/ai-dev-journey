# 子教练日报 · 2026-09-25

> 规则：只写证据，不写"总指挥验收通过"这类结论（验收由总指挥下达）。

## 1. 今日产出

- **文件**：
  - `week06/main.py`（77 行，新建）、`week06/README.md`（106 行，新建）
  - `week05/prompt_lab.py`（W5 尾巴：`else` 分支补内层 `try/except KeyError`）
  - `week05/README.md`（代码围栏闭合，7 → 8 个）
- **commit**：`0b38332`（`style: ruff format week01-week03（统一格式）`）、`f044da4`（`fix: week05 补 JSON 字段缺失兜底 + README 围栏闭合`）、`6ee4445`（`feat: week06 FastAPI /chat 接口（pydantic v2 入参校验 + 错误兜底）`）
- **是否已 push**：是。`git rev-parse HEAD` 与 `origin/main` 同为 `6ee4445475b8db4a00fda4f9c301204e70f3aedb`，`git ls-remote origin main` 一致，`git status --short` 为空
- **我（子教练）自己核对的方式**：
  - `read_file` 读 `week06/main.py`（改了 6 轮，每轮都重读）、`week06/README.md`、`week05/prompt_lab.py`、`week05/README.md`
  - **实跑 `uv run python week06/main.py`** → 退出码 0、秒退、无 uvicorn 日志 ⇒ 证明 README 里那条旧命令**起不了服务**
  - 实跑静态三件：`uvx --offline ruff check .` / `ruff format --check week06` / `pyright --pythonpath .venv/Scripts/python.exe week06`
  - 本机另建探针项目（scratch `faprobe`，**不碰学员仓库**）把 `07` 的骨架真跑一遍：POST `/echo` 无参 → 422 且 `loc:["query","text"]`；`ChatRequest` 各校验分支的 422 body 逐个记录；`/docs` 200；`fastapi 0.141.1 / pydantic 2.13.5`
  - 另用 `pwsh -File test.ps1` 实测 PowerShell 下 `curl.exe -d '{"message":"你好"}'` 与 `--data-binary "@body.json"` 两种写法，避免再犯 09-21「漏引号害学员卡 `>>` 续行」的错
  - `git log` / `git diff` / `git status` / `git cat-file` 核验每个 hash

- **学员自述（今天学会了什么，3 行）**：① GET/POST 两种请求，`ChatRequest` 继承 `BaseModel` 来定义请求体的传参格式与类型限制，再把这个请求体传进自己写的 POST 接口，做成「别人能调用的服务」；② 最大的差别是 FastAPI 智能 —— **自己一行校验代码都没写，它会自动返回校验错误**；③ 还做了上游状态错误 / 超时 / 连接失败的兜底，连自己没写到的请求体参数不符，FastAPI 也自动兜住了。

## 2. 讲回记录

| 讲了什么 | 结果 | 错在哪 / 补了什么 |
|---|---|---|
| 422 body 读法（`type` / `loc` / `ctx`） | 通过 | 第一轮只给猜测「是不是温度太大或类型不符」，要求贴原始输出后才读对 |
| 「校验代码一行没写」+ 函数未被调用 | 通过 | `print("进入 /chat")` 实测：422 时控制台无输出 ⇒ 校验发生在函数之前 |
| key 读取时机 / `--reload` 边界 | 通过 | 学员自答「启动时读一次，除非把读取放在接口里」；子教练补：`--reload` 只盯 `*.py`，改 `.env` 不触发重载 |
| `messages` 的形状 | 通过 | 两轮：先传类（`ModelMetaclass` 不可序列化）→ 再传单字符串 → 对照自己 `week05/run()` 才拼成两条 dict |
| 普通 `def` 路由会阻塞 | 通过 | 「那就等呗，只能一条条处理」 |
| 无状态接口 / history 由谁维护 | 通过 | 原答「加个数据库」；补讲：前端一个数组即可，数据库只在跨会话/跨设备时才需要 |
| 上游 4xx 为什么不能透传 | 通过 | 最终改成 402/429 → 503、其他 → 500 |
| `content` 何时为 `None` | **不通过 → 补讲** | 学员答「没拿到请求就是 None」（答成 4xx/5xx 判据的复述）；正解：模型这一轮没有文本内容时为 `None`，最典型是**工具调用轮**；W6 当异常处理，W17 起要区分 |
| 空回复该返回什么码 | 通过 | 选 502，理由「上游给了无效响应」写进了 README |
| GET/POST、查询参数（`loc:["query","text"]`） | 通过 | 改一处练习：加 `times: int` 并用 `?text=hi&times=3` 调通 |

## 3. 卡点

**已修**

1. `/chat` 首版 500：`messages=ChatRequest`（传了类本身）→ `TypeError: Object of type ModelMetaclass is not JSON serializable`；同版还混用 `stream=True` 与 `.choices[0].message.content`（流式取法用在非流式上）。修法：现场拼 `[{"role":"system",...},{"role":"user",...}]` + `stream=False`
2. W5 遗留：`prompt_lab.py` 的 `else` 只防「不是 JSON」，不防「是 JSON 但字段名不对」（实测 `{"name":"小王","place":"学校"}` → `KeyError` 崩）→ 学员自己写成内层 `try / except KeyError / else`
3. `week06/README.md` 里 `uv run python week06/main.py` 跑不起服务 → 子教练实跑证实后，学员改成 `uv run uvicorn week06.main:app --reload`
4. `ruff format .` 全仓格式化，波及 `handbook/07`（共 9 个文件）→ 07 已 `git checkout` 还原，week01-03 单独提交 `0b38332`

**未修（留给下一轮）**

- 技术项已清空。W7 的内容等总指挥派单。

**体系问题（请总指挥裁定 —— 子教练不自行改制度文件）**

1. **子教练一次把 `07` 的四件 + 收口清单全铺开**，学员多次表示「都没做完就一直急着推进」「你比我还急吗」→ 与「一件过了再展开下一件」的节奏不符。建议在 `handbook/0X` 里写明：**子教练每轮只展开当前一件，收口清单最后才给**。
2. **子教练自称「剩余任务量由你定」「这类杂活我全包」** → 学员当场质疑「你有这个权限吗」。**配量权在总指挥**（`ROADMAP` + `07`），杂活授权应**逐次点名**、不由子教练宣示。建议写进 `.hermes.md` 或 `handbook/00`。
3. 学员口头「改了」两次实际未改（`prompt_lab.py` 的注释块、`README` 的 4 处残留）→ 子教练 `grep` 逐条核验后才发现。**核验动作有效，规则无需改**，但请总指挥注意：**「学员说改了」不能作为收口依据**。
4. 本轮学员提出的一条边界值得记录（子教练执行、总指挥裁定）：**格式/排版类杂活（围栏、空行、取消注释）由子教练直接做，逻辑/接口/数据流类绝不代劳**。今天实际发生过一次（子教练还原了 `prompt_lab.py` 被注释掉的两段实验）。

## 4. 明天第一件事（只许一条）

**等总指挥把 `handbook/07` 重写为 W7**（前端最小页 + fetch/SSE 流式接收，对应 `ROADMAP.md:57`）；**在 07 更新前不自行开新格**。
