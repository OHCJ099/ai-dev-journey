# 子教练日报 · 2026-09-18

## 1. 今日产出

- 文件：
  - `week01/contacts/storage.py` —— 读写 json（`pathlib`：`exists` / `read_text` / `write_text`）+ `json.loads` / `json.dumps(ensure_ascii=False, indent=2)`；2 个函数带类型注解；文件不存在返回 `[]`；坏 json **不在此层捕获**（让异常上抛）
  - `week01/contacts/core.py` —— 4 个纯函数（`add_contact` / `list_contacts` / `find_contacts` / `delete_contact`）+ 类型注解；无 `print` / `input` / 文件读写；`delete_contact` 统一收**用户号**（1-based），越界返回 `None`
  - `week01/contacts/cli.py` —— 5 条命令主循环；无文件读写；`load_contacts` 外包 `try/except json.JSONDecodeError` 并 `return`（防止坏文件被空列表覆盖）
  - `week01/contacts/README.md`（运行方式 + 命令 + 分层说明）、`week01/contacts.example.json`、`.gitignore` 追加 `week01/contacts.json`
- commit：`e1c96ef`（storage 层）→ `7012eb0`（core 层）→ `7dd06e5`（删测试 print）→ `7bc3150`（delete_contact 用户号 + 边界）→ `a4611f3`（cli 层）→ `86e4e81`（contacts.json 移出版本库）
- 是否已 push：**待 push**（收工前执行）
- 我（子教练）自己核对的方式（读了哪些文件 / 跑了哪些命令）：
  - `read_file` 逐行读 `storage.py`（14 行）、`core.py`（20 行）、`cli.py`（72 行）全文
  - `grep -n "print\|input" week01/contacts/storage.py week01/contacts/core.py` → 无输出
  - `grep -n "open(\|read_text\|write_text\|json.load\|json.dump" week01/contacts/cli.py` → 无输出（`DATA_FILE` 出现属合法：仅作参数传递）
  - **对照测试**：`uv run python week01/contacts.py` 与 `uv run python week01/contacts/cli.py` 喂同一串输入（`list` / `find 张` / `find 不存在的人` / `add 新同学 12345678901` / `list` / `del abc` / `del 1 2` / `del 0` / `del 99` / `del 6` / `list` / `quit`），`diff` 两份输出 → **`完全一致 ✅`**
  - 破坏性输入：`delete_contact` 边界 `2`（= len）、`99`、`0`、`-1`、空表 → 全部 `None`，无 `IndexError`
  - `git check-ignore -v week01/contacts.json` → 命中 `.gitignore:15`；`git ls-files week01/` → 已不在版本库
  - 移出版本库后重跑 `printf 'list\nquit\n' | uv run python week01/contacts/cli.py` → 仍能读到 5 条数据

## 2. 讲回记录

| 讲了哪几行 | 结果 | 错在哪 |
|---|---|---|
| 三层职责边界（storage=文件 / core=数据 / cli=人），自比「后厨 vs 服务员」 | 通过 | 术语错：说「cli 抛出异常」，应为「cli **捕获**异常」；抛的是 `int()` / `json.loads()` |
| 「改 `find` 的匹配规则 → 只动 `core`」 | 通过 | — |
| 「为什么 `storage.py` / `core.py` 禁止 `print`」 | 通过（补） | 只答出「多次处理会重复输出、污染输出」；漏掉根本理由：`core` 不知道将来谁在展示（终端 / 网页 JSON），写死 `print` 后换展示方式必须回头改 `core` |
| 「json 换成 sqlite 改哪几个文件」 | 通过（补） | 答「只改 storage」正确；补精确说法：只重写 `load_contacts` / `save_contacts` 的**内部实现**，**签名不变**，`core` / `cli` 零改动 |
| 「`contacts.append(...)` 只应出现在哪个文件」 | 通过 | — |
| `Path(__file__)` 指什么 | **通过**（子教练当场判「不通过」是**误判**，总指挥 2026-09-18 复核纠正） | 学员答「去一层 = `...\week01\contacts`；去两层 = `...\week01`」——**实测完全正确**。子教练误判的原因：它把学员答的「1./2.」理解成「第 1 个 `.parent` / 第 2 个 `.parent`」，而学员答的是「去一层 / 去两层」。**根子是子教练的题面有歧义**（「去掉第二个」可两读），责任在教练不在学员。总指挥用 `Path('storage.py').resolve()` 实跑核对，学员的路径层级记忆是准的 |
| 数据流向（`storage` 读 → `cli` 收输入 → 分支 → `core` 处理 → 回 `cli` 打印） | 通过 | 能按代码逐段说清；自评「原来对这个流程是模糊的」 |

**3 行「今天我学会了什么」（学员原话压缩）**：
1. 我能说清 `def` 的类型注解：参数要什么类型、返回什么类型都写在签名上（`contacts: list[dict[str, str]]`、`-> None`），以前完全不知道有这东西。
2. 我能说清三层之间的数据怎么流动：`cli` 先把文件读进内存 → `input()` 拿命令 → 进对应分支 → 调 `core` 的函数处理 → 返回 `cli` 打印。
3. 今天踩的坑：`find_contacts(contacts)` 少传一个参数报 `TypeError: missing 1 required positional argument`，找了半天 —— 教训是**报错最后一行直接告诉你缺什么**，先读它，别急着翻代码。

## 3. 卡点

- 已修：
  - `core.py` 测试用 `print` 写在函数外 → **import 即执行**（第 2 次犯，与 `storage.py` 同一个错；已删并提交 `7dd06e5`）
  - `delete_contact` 边界 **off-by-one**：`len(contacts) >= index >= 0` 放宽一格，`index == len` 时 `pop` 越界 → `IndexError`。改为收用户号 + `1 <= user_no <= len(contacts)`
  - `cli.py` 输出格式漂移 3 处（序号缺 `.`、`已删除` 多冒号、`格式：` 全角冒号）→ 对照测试抓出并修正
  - `except ValueError` 后漏 `continue`（W1 犯过，本次**自行发现补上**）
  - `find_contacts(contacts)` 漏传 `keyword` → `TypeError`
  - `if text == None:` → 应为 `if not text:`（`[]` 不等于 `None`，但都是 falsy）
  - `readme.md` 大小写、`contacts.example.json` 放错目录、PowerShell 里粘 bash 语法（`<` 报 `ParserError`）
  - 无意删掉 `cli.py` 里有意的 import 分组空行（PEP8：标准库 / 第三方 / 本项目三组分隔）—— 已恢复
- 未修（留给下一轮）：
  - **「抛出 / 捕获」术语未稳**：今日两次说反，需在下一轮随机抽查
  - 全角 / 半角标点仍会手滑（W1 犯过）
  - 输出格式一致性目前只靠**人工对照测试**兜底，尚无自动化检查
  - `ruff check` 未跑（W2 验收标准之一）

## 4. 明天第一件事（只许一条）

装 `ruff`（`uv add --dev ruff`）并让 `uv run ruff check week01/` 无输出 —— 这是 W2 的验收标准之一，且今天讨论过的 import 分组、逗号前后空格会被它直接抓出来。
