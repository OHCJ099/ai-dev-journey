# 进度台账

> 规则：**每个学习日结束，子教练在这里追加一行**。总指挥官按这张表验收。
> 写法要求：产出必须是可核对的东西（文件名 / commit hash / 链接），不许写「学习了 xxx」。

| 日期 | 阶段 | 产出（文件/commit） | 讲回情况 | 卡点 | 备注 |
|---|---|---|---|---|---|
| 2026-09-15 | ① 环境 | `.gitignore`+仓库初始化，commit `871b141`，GitHub `OHCJ099/ai-dev-journey` | — | PowerShell 不认 `ls -a`（bash 语法） | 项目与 Hermes 家目录搬到 D 盘 |
| 2026-09-16 | ① Python | `week01/contacts.py` v1：add/list + 参数校验，commit `1a4ce70` | ❌ 讲不出来 | 抄完不知道在干嘛 → 触发体系重做 | 已建立三步法与子教练体系 |
| 2026-09-16 | ① Python | json 持久化，commit `c506042`（含 `contacts.json`） | ✅ 已讲回（15:10 补做，5 行逐行讲通） | `append` 贴错分支（缩进=分支归属） | `find` 未完成 |
| 2026-09-16 | ① Python | `week01/contacts.py` find 分支：`startswith` + 参数校验 + 标记变量 + "没找到"提示 + 未知命令兜底，commit `dc0d00a`（已 push） | ✅ 讲回通过（31–41 行逐行讲对） | 两次"改了没存盘"；`found = False` 写在循环内 → 搜第一条会多打一行"没找到" | 学习期关掉 VS Code AI 补全（卸载 Qoder CN / TraeCode + 全局 settings）；序号与「find 李 包含匹配」顺延 Day3 |
| 2026-09-17 | ① Python | `contacts.py`：`find` 改 `in` 包含匹配 + 打印电话；`list`/`find` 带序号（`enumerate start=1`）；`del` 分支（`ValueError` 兜底 + 链式范围校验 + `pop` 取回被删记录）；commits `5c2399d`/`b431d0c`/`5d3be86`/`7c9add4`（已 push）。另：`notes/README.md` 笔记规范 | ✅ 6 项讲回全过（`in` vs `==`、`start=1`、`try` 范围、链式比较、下标 −1、`pop` vs `remove`） | 改了没保存 1 次；**存了跑了忘了提交 1 次**（`5d3be86` 缺 `continue`，由 `7c9add4` 补上） | W1 三项返修全部结清；明天开始拆模块 + 类型注解 |
| 2026-09-18 | ① Python | `week01/contacts/` 三层拆分完成：`storage.py`（pathlib 读写 json）/`core.py`（add·list·find·delete 四个纯函数 + 类型注解）/`cli.py`（5 条命令主循环，无文件读写）；另 `week01/contacts/README.md`、`week01/contacts.example.json`、`.gitignore`（`week01/contacts.json` 移出版本库）。commits `e1c96ef` / `7012eb0` / `7dd06e5` / `7bc3150` / `a4611f3` / `86e4e81` | ✅ 4 题讲回：三层职责边界 ✅、「改 find 匹配规则只动 core」✅、「换 sqlite 只动 storage」✅、「`contacts.append` 只应出现在 core」✅。2 处当场纠正：①「cli 抛出异常」应为「cli **捕获**异常」（抛的是 `int()` / `json.loads()`）；②~~「`Path(__file__)` 是目录」说反了~~ —— **此条为子教练误判，总指挥 18:45 复核撤销**（学员答「去一层/去两层」完全正确，是题面「去掉第二个」有歧义；详见 `logs/2026-09-18-日报.md`） | ① `delete_contact` 边界 off-by-one（`len >= index >= 0` 放宽一格 → `IndexError`），改为收用户号 `1 <= user_no <= len`；② `print` 写在函数外 → import 即执行（**第 2 次犯**，已删）；③ 输出格式漂移 3 处（序号缺 `.`、`已删除` 多冒号、`格式：` 全角冒号），由对照测试抓出；④ `except ValueError` 漏 `continue`（W1 犯过，本次自行补上）；⑤ `find_contacts(contacts)` 漏传 keyword；⑥ `if text == None` 应为 `if not text`（`[]` ≠ `None`）；⑦ `readme.md` 大小写 / `example.json` 放错目录 / PowerShell 里粘 bash 语法 | **对照测试通过**：老 `contacts.py` 与新 `cli.py` 喂同一串输入，`diff` 输出「完全一致」。`ruff check` 未做（W2 验收标准）。**Ctrl+S 存盘问题今天 0 次**（连续 3 天后首次） |

## 战略调整记录 · 2026-09-17 14:15（总指挥）

学员提供了三项关键信息，排期据此重排：

1. **毕业时间**：2027-06-01（257 天 / 36.7 周），不是此前假设的 6 个月 → 真正的死线是 **2027-02-01 春招开窗**（136 天 / 19.4 周）
2. **可投入时间**：大四上仅 3-4 节课/周（5.2h），一周空闲约 97h → 投入从 20h/周 提到 **28h/周**（起步两周 25h 试运行）。26 格内容量约 520h → 约 18.6 周完成（2027-01-28）
3. **毕设**：可自选技术栈（待导师开会确认）；学员明确**不学 openGauss**（地域生态窄、与就业方向不符，判断合理，已接受）；策略定为 **毕设 = 项目②（知识库问答）**，代码自己写、论文用 AI 辅助

产出：
- `ROADMAP.md`：内容排期增加「完成日」列 + 里程碑表 + 28h/周节奏与疲劳安全阀
- `docs/求职时间线.md`（新增）：关键窗口、投递节点、渠道与搜索词、投递频率、拿到实习后的处理、毕设策略
- `.hermes.md`：加入死线提醒

## 战略调整记录 · 2026-09-17 15:00（总指挥 · 外部审计后）

学员把体系总结交给 Kimi 做外部审计，收到 5 条风险 + 学员自己的两点顾虑（怕 AI「强行批判」、担心文档越改越冗余）。逐条判定后采纳 4 条、驳回 1 条，并补 2 条审计漏项：

**采纳**：
1. **砍单阶梯**（新增，写入 `ROADMAP.md`「一」）：W22 Ollama → W21 MCP → W19-W20 LangGraph → W17-W20 项目③ → 最后才动 RAG 打磨。理由：Kimi 指出「安全阀说拉长周期、死线说不能拉长」的仲裁缺失，成立。
2. **疲劳与进度正交**（新增仲裁规则）：疲劳 → 必须降强度（20h/周）；进度落后 → 只砍内容，不砍死线不加时长。一句话：**永远不用健康换死线，只用内容换死线。**
3. **每周子教练合规审计**（写入 `handbook/06` 第五节）：5 条红线逐条核，10 分钟。理由：目前规则靠学员上报，而学员判别力逐周长成，愤怒是滞后信号。
4. **W24 模拟面试改隔离会话**（写入 `handbook/05`）：普通模拟里 AI 知道学员学过什么，只会问他会的东西。另建议 12 月起找真人 mock。
5. **被拒心态预期 + Hermes 单点备份**（写入 `docs/求职时间线.md`）：前 30-50 份被拒是数据采集；每月导出会话库。

**驳回**：Kimi 建议「按小时给 26 格标两级（最小集/可砍格）」—— 19.7h/格是估算不是实算，在假数字上做分级是假精确。改为：**W3 结束（10-05）用实测耗时做第一次校准**，届时再决定砍哪档（校准点写入 `handbook/06` 第六节）。

**修正 Kimi 的两处**：
- 它用 W1 实际消耗（8.3h）外推说排期乐观 —— W1 是最简单一格且含 3-4h 一次性成本，**不可外推**，正反都不能用。
- 它说「512h 是实算」—— 是估算，所以「6.7% 缓冲」本身是假精确，真实误差可能 ±20%。结论一致：**有砍单预案才扛得住误差**。

**审计漏项（比它列的 5 条更紧急）**：
1. **毕设被否的风险**：09-21 导师开会。若选题被否或强制指定技术栈，毕设从「零额外工作量」变成 +80~120h，缓冲当场变负 → 已写 Plan B（4 档，写入 `docs/求职时间线.md`）
2. **面试题库暴露概念地图缺口**：学员提供约 100 题题库，比对后约 25 个概念表里没有 → 新增 `docs/面试题库.md`，并在 `notes/概念地图.md` 加 `🔵 面试认知级` 标记（能口头讲清即可，不实现），**不增加工作量**

**发现的文档问题（防屎山）**：
- `ROADMAP.md` 第 15 行「512 小时」与第 23 行「520h」并存 → 统一为 520h / 18.6 周
- 统计重复：「2027-02-01」出现在 11 个文件、「28h/周」6 个、「三步法」10 个 → 在 `handbook/00` 新增「单一事实来源」规则表，规定每个事实的唯一权威位置

**产出**：`ROADMAP.md`（砍单阶梯 + 仲裁规则 + W24 改题库）、`docs/面试题库.md`（新增，136 行）、`docs/求职时间线.md`（毕设 Plan B + 被拒预期 + 备份）、`handbook/00`（单一事实来源）、`handbook/05`（隔离 mock）、`handbook/06`（合规审计 + 校准点）、`notes/概念地图.md`（🔵 档 + 约 25 条挂载）、`README.md`（重写，去掉 openGauss、更新死线与节奏）、5 份子教练提示词（投入数字同步）

## 待验收项（总指挥官关注）

- [x] 学员能否不看代码讲清 `add` 分支那 5 行（dict / split / len / continue / append）—— 09-16 15:45 通过
- [x] `find` 是否实现（含"没找到"提示）—— 09-16 16:15 实跑 `find 李四 / 王五`，各只输出一行，commit `dc0d00a`
- [x] `del` 是否实现（含 ValueError / IndexError 兜底，`enumerate` 序号）—— 09-17 实跑 `del abc / del 99 / del 1` 全通过
- [x] `list` / `find` 是否带序号（`enumerate`，`del` 的前置条件）—— 09-17 实跑通过
- [ ] "改完先 `Ctrl+S` 存盘再运行"是否形成习惯 —— 09-16 两次、09-17 一次，在减少但未定型

## 总指挥验收记录 · 2026-09-16 16:25（第 1 轮）

**我自己实跑的**（不听汇报，只认输出）：

```
$ printf 'find 李\nfind 王\nfind 李四\nlist\nquit\n' | uv run python week01/contacts.py
> 没找到李          ← 应该命中「李四」，实际没有
> 没找到王          ← 应该命中「王五」，实际没有
> 找到了李四!       ← 命中了，但没打印电话
> [{'name': '李四', 'phone': '139'}, {'name': '王五', 'phone': '138888'}]
```

**判定：阶段① W1 不通过**，差异如下：

| 项 | 结果 |
|---|---|
| 讲回（两轮：add 5 行 / find 31–41 行） | ✅ 通过 |
| `find` 存在、能跑、"没找到"提示、未知命令兜底 | ✅ |
| **`find` 用 `==` 精确匹配，规格要求「关键字包含」** | ❌ 打回 |
| **`find` 只打印「找到了X!」，没打印电话** | ❌ 打回 |
| `del`、`list`/`find` 序号（`enumerate`） | ❌ 未做 |
| 关掉 VS Code AI 补全（学习期自己写） | ✅ 好决定，保留 |

返修任务已写入 `handbook/07-当前任务指令.md`，子教练照它带下一轮。返修完成后我复验。

## 总指挥验收记录 · 2026-09-17 13:45（第 2 轮）

**我自己实跑的**（用你 `contacts.py` 的副本在临时目录跑，不动你的数据）：

```
$ printf 'add 李雷 111\nadd 韩梅梅 222\nlist\nfind 李\nfind 王\nfind 赵六\ndel abc\ndel 99\ndel 1\nlist\nquit\n' | uv run python contacts.py
> 1. 张三 137 / 2. 李雷 111 / 3. 韩梅梅 222     ← list 带序号 ✅
> 2. 李雷 111                                   ← find 李：包含命中 + 序号 + 电话 ✅
> 没找到王 / 没找到赵六                          ← 提示用的是变量，没有写死值 ✅
> 请输入数字                                    ← del abc 不崩 ✅
> 数字不合法                                    ← del 99 不崩 ✅
> 已删除 张三 137                               ← pop 取回被删记录 ✅
> 1. 李雷 111 / 2. 韩梅梅 222                    ← 删除后重新编号 ✅
重开进程再 list → 李雷 / 韩梅梅 仍在（删除已存盘）✅
```

**判定：功能项全部通过**，W1 三项返修结清。概念项等学员现场回答 3 题（`==` vs `in` / `enumerate` 的 `start` / `pop` vs `remove`）后正式收口。

流程问题（记录在案，不扣功能分）：
- 改了没保存 1 次（09-16 是 2 次，在减少）
- **存了、跑了、忘了提交 1 次**：远端曾有一版 `5d3be86` 缺 `continue`（`except` 后直接往下走会 `NameError`）。教训：能跑的版本立刻提交，别把修复留在本地。
- `del` 越界提示语为「数字不合法」，规格里写的是「序号不存在」——意思等价，接受。

已知未做（下一步）：拆分成 `storage / core / cli` 三文件 + 类型注解，见 `handbook/07-当前任务指令.md`。

## 概念收口记录 · 2026-09-17（学员现场作答，总指挥亲耳听）

- **第 1 题 `==` vs `in`：✅ 通过**（原话：「`==` 判断字符串是不是完全一样，`in` 是字符串在不在字符串内、是不是它的一部分」）。
  总指挥补充要点：`in` 左边是**关键字（用户输入）**，右边是**数据里的姓名** —— 这条分界就是「用户输入 vs 数据」。三个月后 RAG 检索同一件事：用户的问题去比对文档片段。
- **第 2 题 `enumerate(..., start=1)` 为什么必要：⏳ 漏答**，已要求补讲。给的思考题：去掉 `start=1`，`list` 会打印成什么样子？那样 `del 1` 会删掉谁？
- **第 3 题 `pop` vs `remove`：✅ 通过**（原话：「`remove` 删的是对应的值，`pop` 删的是索引；`pop` 会返回删掉的值，`remove` 似乎没有给我返回东西」）。
  总指挥补充：`remove` 返回 `None`（不是"似乎"），可用 `print([1, 2].remove(1))` 一行验证；另外 `remove` 只删**第一个**匹配项，值不存在会抛 `ValueError` —— 这正是 `del` 用 `pop` 而不是 `remove` 的原因（按序号删，还能拿回被删记录用来打印）。

**W1 收口：已于 2026-09-17 通过**（第 2 题补答，见下）。

### 第 2 题补答与判定

学员原话：「会打印成 0 1 2 3 而不是 1 2 3 4；`del 1` 会删掉列表第一个，但 `list` 从 0 开始显示，假如用户要删的是第二个人，输入 `del 1` 删的却是第一个。」

**判定：✅ 通过。** 关键在他给出的是一条**具体后果**（删错人），不是背定义 —— 排查 off-by-one 就应该这样推。

**W1 正式通过。** 沉淀一句话（建议写进 `notes/concepts.md` 的概念卡）：

> 显示编号 ≠ 列表下标。`enumerate(..., start=1)` 让「用户看到的号」和「用户的预期」对齐；代码内部要用下标时再 `-1` 换算。三个坐标系（用户号 / 显示号 / 下标）必须显式对齐一次，否则删错人、取错数据。

### W1 成绩单

| 项 | 结果 |
|---|---|
| 交付 | `week01/contacts.py`：`add`/`list`/`find`/`del`/`quit` 存盘 + 输入校验 + 越界兜底 + 序号显示 |
| commits | `1a4ce70` → `c506042` → `dc0d00a` → `5c2399d` / `b431d0c` / `5d3be86` / `7c9add4` |
| 讲回 | 通过：add 5 行、find 8 行、json 加载 3 题、del 相关 6 项、`==` vs `in`、`pop` vs `remove`、`enumerate start` |
| 学会的三件事（子教练压缩） | 缩进=分支归属；字面量 vs 变量；用户输入 vs 数据 |
| 差点栽的 | ① 改了没存盘（3 天里 3 次 → 在减少）② 忘提交导致远端一版缺 `continue` |
| 下一阶段 | W2：拆分 `storage / core / cli` + 类型注解 + `contacts.json` 移出版本库（`handbook/07`） |

## 总指挥验收记录 · 2026-09-18 18:35（第 3 轮 · W2 第 1 格）

**判定：分层与行为全过；两项尾巴未收（`ruff` 未跑、`notes/` 错题本未建）。**

**总指挥自己实跑的证据**（不听转述）：

1. **新旧行为对照**：把老 `contacts.py` 与新 `cli.py` 喂同一串输入（`list` / `find 李` / `find 王` / `add 测试 999` / `list` / `del abc` / `del 99` / `del 0` / `del -1` / 空行 / 空格 / `foo` / `quit`），`diff` → **完全一致 ✅**（数据文件每轮重置，排除写盘干扰）
2. **破坏性输入**（副本跑在临时目录，不动学员数据）：`del 3.5` → `请输入数字`；`del ０１`（全角）→ int 接受全角数字，删掉第 1 条；`del +1` → int 接受正号，删掉第 1 条；`add a b c` → `格式`；`add 张三<TAB>139` → `split()` 吃制表符，正常入库；`add 😀😀 123` → 正常；空列表时 `list` / `find` / `del 1` → 均无异常；**损坏的 json** → `数据文件损坏` + `return`（不覆盖原文件）→ 退出码 0
3. **分层检查**：`grep print\|input` 于 `storage.py` / `core.py` → 无输出；`cli.py` 无文件读写
4. **工程卫生**：`git ls-files` 里已无 `week01/contacts.json`；`__pycache__` 未被跟踪
5. **`ruff check`（总指挥代跑，`uvx ruff`）**：`week01/contacts/cli.py:1` → **I001 import 未排序**（1 个，可 `--fix`）。同一组内要按字母序：`core` 在 `storage` 前；行内名字也按字母序，且常量优先（`DATA_FILE` 在前）

**扣分项**

| 项 | 说明 |
|---|---|
| `ruff check` 未跑 | 这是 W2 明确验收标准，子教练已列为明天第一件事 —— 允许 |
| `notes/gotchas.md` / `concepts.md` 未建 | W2 验收标准之一，且今天日报里已攒了 7 条现成素材（含 2 次重复犯错） |
| 旧 `week01/contacts.py` 未删 | 与新实现并存；行为已被证明一致，旧文件完成使命，应删（git 历史里永远留着） |
| `week01/contacts/__pycache__/core.cpython-312.pyc` | 说明至少有一次是用**系统 python 3.12** 跑的，不是 `uv run`。不影响结果，但违反「永远用 uv run」 |
| 术语「抛出 / 捕获」说反 2 次 | 上一轮已记录，本轮仍未稳，下一轮随机抽查 |

**注意**：本轮日报的「3 行今天我学会了什么」质量高于 W1 —— 三条都是可检验的事实（类型注解写在签名上 / 数据流向 / 报错最后一行直接告诉你缺什么），没有一句「学习了 xx」。

**18:45 更正（总指挥自我纠错）**：本轮抽查第 1 题（`.parent` 层级），子教练当场判学员「不通过」，**是误判**。学员答「去一层 = `...\week01\contacts`；去两层 = `...\week01`」实测完全正确；误判原因是**题面「去掉第二个」有歧义**（可读成「去掉第 2 个 `.parent`」）。已在 `logs/2026-09-18-日报.md` 讲回表与 `progress.md` 台账行标注撤销。

**这是三天里第 2 次判错学员**（第 1 次：09-17 示例输出写了具体值「王」，学员抗议正确）。**教训**：教练出题必须无歧义；判定「不通过」前先自检题面；学员反驳时先复核自己，不要默认学员错。拟新增一条硬规则（待学员点头）：**「判定不通过前，先确认自己的题面只有一种读法；学员反驳时优先复核自己。」**
