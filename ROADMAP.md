# 六个月蓝图 v2（总指挥官版本）

> 用法：这份文件是**总纲**。每个阶段开一个新对话当「子教练」，提示词在 `handbook/`。
> 进度台账：`progress.md`（每个学习日由子教练追加一行）。验收标准见 `handbook/06-验收协议.md`。
> 学员坐标看文末「当前坐标」。

## 一、总目标与硬指标

**真正的死线不是毕业（2027-06-01），是 2027 春招开窗（2027-02-01）。** 距 2026-09-22 还有 132 天 ≈ 18.9 周。

6 个月内做出 **3 个可演示项目**（对话应用 → RAG 知识库问答 → Agent 多工具任务），在春招开窗前完成简历与面试准备，投递初级 AI 应用工程师。

唯一的硬指标：**不看答案，你能写出多少。** 抄得再多不算数。

**投入**：28 h/周（工作日 4h + 周末 4-6h），26 格内容量约 520h → 约 18.6 周完成（2027-01-28，比春招开窗早 4 天）。
起步两周（9/22-10/4）先跑 **25 h/周** 试运行，10/5 复盘再提到 28。

**疲劳安全阀**（出现任一信号就回调，不许硬扛）：① 连续 2 天坐下先刷 30 分钟手机；② 任务做完但讲不出自己写了什么；③ 周中就开始盼周末。

**疲劳与进度是两件正交的事，分开处理（仲裁规则）：**

| 情况 | 处理 |
|---|---|
| **疲劳**（安全阀任一信号） | **必须降强度**（20h/周），健康优先，不许硬扛 |
| **进度落后** | **只砍内容**，不砍死线、不加时长、不熬夜 |

一句话：**永远不用健康换死线，只用内容换死线。**

**砍单阶梯**（现在冷静时定死，落后时照单执行，不许临时决策）：

| 顺序 | 砍什么 | 代价 |
|---|---|---|
| 1 | W22 的 Ollama 半格 | 无（面试题可口头答） |
| 2 | W21 的 MCP 半格 | 小（认知级即可） |
| 3 | W19-W20 的 LangGraph 版 | 中（项目③降级为手写 ReAct Agent） |
| 4 | W17-W20 项目③ 整体 | 大（只交 2 个项目，但项目①②完好） |
| 5 | **RAG 打磨（W15）** | **最大 —— 尽量不动，RAG 是核心筹码** |

**校准点**：W3 结束（2026-10-05）用实际耗时反推剩余格子的预计完成日 —— 这是第一个可用的速度数据点（W1 是 26 格里最简单的一格且含一次性成本，不可用于外推）。若届时预计完成日 > 2027-01-28，按上表从第 1 档开始砍。

## 二、内容排期（26 格内容不变）+ 日历映射（按 28h/周）

**内容格不砍（除非触发砍单阶梯）。** 26 格内容共 520h，压缩进约 18.6 个日历周 —— 下表右列是每格的实际完成日期（W2 从 2026-09-22 起算）。

> **实测校准（总指挥 2026-09-22）**：W3 全格实测 **2h55**（预算 5 天 × 4h = 20h）；W4 第 1 件实测 **1h20**。早期格的用时**远低于**预算。
> **处理方式 —— 不动里程碑，把省下的时间投进「深度要求」**（见下），**不把日期前移**。理由：项目① 的质量比「早两周完成」值钱得多；且前几格好推不代表 RAG 好推。**10-05 校准点**再决定 W9 之前的日期要不要整体前移。
> **已知代价**：按实测速度，W4-W7 会在 10-05 前后收完，到 W8（10-30 项目①）之间会有一段空档 —— 那段空档**明确用于项目① 的深度打磨**，不许拿来提前开 RAG。

| 周 | 阶段 | 学什么 | 交付物 | 验收标准 | 完成日 |
|---|---|---|---|---|---|
| W1 | ① Python 基础 | 环境/uv/git、list/dict/循环/if、文件 IO、异常 | `week01/contacts.py`（增删查+json 持久化） | 能不看代码讲清每行；能改一处加功能 | ✅ 09-17 |
| W2 | ① | 函数、模块分层、类型注解、`pathlib`、`.env`、`httpx` | contacts 拆成 `storage/core/cli`；`notes/` 错题本 + `test_contacts.py`（原生 assert） | `ruff check` 无错；分层清楚（storage 里没有 print）；畸形 json 不崩且不覆盖原文件；**意外退出（EOF/关窗口）不丢数据** | ✅ **09-20**（Ctrl+C 一条挂 W3 第 1 件） |
| W3 | ② 大模型 API | DeepSeek API 首调、messages/role、token 与成本、错误处理；**+ 异步 `async/await` 认识（W6 前置）** | `week03/hello_api.py` + `week02/first_request_async.py` | 口答 system/user/assistant、stream 差别 ✅；401/超时都不崩 ✅；讲清 `async/await` 解决什么 ✅ | ✅ **09-21**（temperature 归 W5 温度实验；流式代码与 `chat_cli v0.1` 顺延 W4） |
| W4 | ② | 多轮上下文管理、历史裁剪、参数化（argparse）、日志 | `chat_cli` v1 完整版 + README | 连续 10 轮对话不走样；`/clear` `/system` `/save` 可用；有 token 统计 | 10-10 |
| W5 | ② Prompt + 后端 | Prompt 基础：角色设定、少样本、结构化输出(JSON)、温度实验 | `week05/prompt_lab.py`（同一问题的多 prompt 对比） | 能讲清每种手法的适用场景；实验有输出对比表 | 10-15 |
| W6 | ② | FastAPI 最小：GET/POST、**Pydantic v2 模型（独立讲回）**、uvicorn、`async def` 路由 | 把对话能力包成 `/chat` 接口 | `curl` 能调通；能讲清「接口」和「函数」的区别；能讲清 pydantic 校验失败时返回什么 | 10-20 |
| W7 | ② | 前端最小页（单 HTML + fetch 流式/SSE） | `chat_web`（浏览器聊天页） | 浏览器里能连续聊天、有流式效果 | 10-25 |
| W8 | ② | 项目①定稿：README、演示、错误兜底、成本控制 | **项目①：带 Web 界面的多轮对话应用** | 别人照着 README 能跑起来；能 3 分钟讲清架构 | **10-30** ← 起投日常实习 |
| W9 | ③ RAG（核心） | 文档加载与切分（chunk 策略）、清洗；**语料 = 企业制度文档（毕设 #91 用）** | `projects/rag-kb/` 雏形 | 能讲清 chunk 大小/重叠怎么选，切分结果能人工检查 | 11-04 |
| W10 | ③ | Embedding 与向量库（Chroma）；相似度与检索 | 能对文档提问并召回片段 | 能讲清向量检索原理（不要求数学推导）；召回质量有主观判断标准 | 11-09 |
| W11 | ③ | 拼上下文、引用来源、防幻觉、拒答；**+ Prompt Injection 与 OWASP LLM Top 10 认知级** | 带引用来源的问答 | 答案能标出处；资料里没有的问题会说「不知道」 | 11-14 |
| W12 | ③ | 检索优化：多路召回/重排/查询改写；评估集 | 20 条问题→答案的评估集 + 命中率 | **有数字**：命中率/正确率前后对比 | 11-19 |
| W13 | ③ | 工程化：上传文档、索引管理、增量更新 | 可上传文档的知识库服务 | 上传新文档后能被检索到；删除后不再召回 | 11-24 |
| W14 | ③ | 前端 + 接口（复用 W6-W7） | 知识库问答 Web 应用 | 浏览器可上传、提问、看引用 | 11-29 |
| W15 | ③ | 调优与成本：token 花费、缓存、失败案例分析 | 优化记录（前后对比） | 能讲清 3 个失败案例的原因和修法 | 12-04 |
| W16 | ③ | 部署：局域网/内网穿透/云主机；演示录屏 | **项目②：知识库问答（可访问）** | 有可访问链接（或录屏）+ README + 架构图 | **12-09** ← 简历主力项目就位 |
| W17 | ④ Agent | Function calling / 工具调用协议 | `week17/tools_demo.py`（手写 2-3 个工具） | 能讲清模型是怎么"决定"调用工具的 | 12-14 |
| W18 | ④ | 手写 ReAct 多步循环、错误重试 | 能多步完成任务的脚本 | 能讲清循环终止条件、防止死循环 | 12-19 |
| W19 | ④ | LangGraph 状态机、记忆（短期/长期） | LangGraph 版 Agent | 能讲清状态图和手写循环的差别 | 12-24 |
| W20 | ④ | 接上 RAG（把项目②当工具）、多工具编排 | **项目③：多工具 Agent** | 能自主「查知识库→调工具→汇总」完成一个复合任务 | **12-29** ← 三个项目齐 |
| W21 | ⑤ 工程化 | 配置/日志/超时重试/限流/成本、pytest 基础；**+ Langfuse 可观测性（半格）**；**+ MCP 协议（`03-3`，半格）** | 三个项目统一工程规范；一个自己写的 MCP server | 有测试、有日志、有错误兜底；能讲清 MCP 与 function calling 的区别；能追一次完整调用链路 | 01-03 |
| W22 | ⑤ | **+ Ollama 本地模型（`01-2`，半格）**；Docker 基础 + 部署到一个公网可访问的地方 | 项目②或③公网链接 | 链接能打开、能用；本地模型能跑起来并说明「什么时候用本地、什么时候用 API」 | 01-08 |
| W23 | ⑤ 求职 | 简历（1 页）、项目描述量化、GitHub 主页整理 | 简历 + GitHub 整理完 | 每个项目有：一句话定位/截图/技术栈/难点 | 01-13 |
| W24 | ⑤ | 面试题库：过完 `docs/面试题库.md` 全部题目（约 100 题，含场景设计题）+ 模拟面试 | 答不上的题补掉 + 面试记录 | 模拟面试能过 80%；题库中「面试认知级」概念全部能口头讲清 | **01-18** |
| W25-26 | ⑤ | 投递、复盘被拒原因、补短板 | 投递记录表 + 复盘文档 | 每周投递量达标；根据反馈补 1-2 个短板 | **01-28** ← 春招前收工 |

**日历 vs 内容周说明**：右列日期是「按 28h/周 匀速推进时该内容格完成的日期」，不是日历周编号。若中途拿到实习或触发安全阀，日期整体后移，内容格不动。

**`W1`~`W26` 是「内容格编号」，不是日历周。** 一格 = 一个知识块，实际用 **1-3 天**（实测：W1 用 3 天、W2 用 3 天、W3 用 1 天），不是 7 天。所以 `weekNN/` 文件夹（代码存放）与 `W NN`（内容格）**都指「第 N 个知识块」**，与「过了几周」无关 —— 第 8 天出现 `week04/` 是正常的。**W9 起代码搬到 `projects/`，不再用 `weekNN/`。**（2026-09-22 学员提问后补注）

## 二·一、深度要求（每格通用 —— 这是把「1 小时做完」补成「3-4 小时做完」的那部分）

**每格不只做「跑通」，必须做满下面三层才算完成。** 只有第一层 = 任务量偏少，子教练次日必须加量。

| 层 | 内容 | 例（以 W4 多轮上下文为例） |
|---|---|---|
| **1. 跑通** | 功能能用，验收标准过 | 连聊 3 轮后答对「我第一句说了什么」 |
| **2. 改一处** | 不看原答案，独立加一个小变体（**允许查文档**） | 加 `/system <角色>` 命令，换角色后历史保留但人设变 |
| **3. 破坏性测试** | 空输入 / 超长 / 特殊字符 / 越界 / 断网 / 连续操作，**看真实报错** | 输入 10000 字；输入 emoji；`/clear` 后立刻再问上一轮的事 |

**外加两条**（W9 起同样适用）：
- **讲回闸门**：说不清就补讲，不往下走（`.hermes.md` 硬规则 2）
- **失败记录**：撞到意外报错才记 `notes/`，不设配额（`.hermes.md` 5·1）

**为什么加这一节**：2026-09-22 学员连续三天 1-3 小时收工，抗议「任务量偏少」。根因是旧规则「一次只给一个任务、提前做完就停」+ 估时按想象。**深度要求是配量器，不是新增制度。**

## 二·二、里程碑（只看这五个日期）

| 日期 | 事件 | 要做什么 |
|---|---|---|
| **2026-10-30** | 项目① 完成 | **开始投日常实习**（别等三个项目齐）；简历 10-20 前先出第一版 |
| **2026-12-09** | 项目② RAG 完成 | 简历主力项目就位，更新简历 |
| **2026-12-29** | 项目③ Agent 完成 | 三个项目齐 |
| **2027-01-18** | 面试准备完成（题库 + 模拟） | 投递弹药备好 |
| **2027-02-01** | 内部目标日 | 主投「实习转正」岗，加大投递量（**实际以公司公告为准**） |

详见 `docs/求职时间线.md`。

## 三、为什么第 1 个月要写「通讯录」这种土程序

因为**它就是 RAG 项目的骨架**，只是把「人」换成了「文档片段」：

| 通讯录里的写法 | 在 RAG 知识库问答里对应什么 |
|---|---|
| `contacts = []` | `chunks = []` 切好的文档片段集合 |
| `add 张三 138` | 往知识库里添加一份文档 |
| `{"name":"张三","phone":"138"}` | `{"text":"...","source":"手册.pdf","vector":[...]}` |
| `find 李`（名字含「李」即命中） | **检索** —— RAG 的心脏 |
| `del 1` | 删除某个文档/片段 |
| `json` 存盘、下次还能读 | **持久化**向量库/索引 |
| `try/except` 不崩 | API 超时/余额不足/限速处理 |
| 拆成 `storage/core/cli` | 项目分层（面试必问） |

一句话：用你完全能理解的数据，把后面要用的零件先过一遍手。

## 四、每天的三步法（防「抄完什么都没学」）

1. **抄**：照着写一遍，建立手感
2. **讲回**：用自己的话把每行讲一遍（最容易跳过，最值钱）
3. **改**：改一处或加一个小功能（从「复制」变成「写出」）

时间分配：理解/讲回 30 分钟 + 动手 90 分钟。宁可少写一个功能，也要把抄过的讲清楚。

## 四·二、每周固定节奏（28h/周）

| 时段 | 干什么 | 时长 |
|---|---|---|
| 周一-周五 | 主线任务（三步法） | 4h/天 |
| 周六 | 主线 + 收工提交 | 4h |
| 周日 | 主线收尾 + **泛读窗口**（见 `notes/README.md` 轨道 B）+ 10 分钟笔记复习 | 4-6h（其中泛读 30 分钟） |

**起步两周（9/22-10/4）先跑 25h/周**，10/5 复盘再提到 28 —— 别一上来就顶满，前两周是测试自己的节奏，不是测试意志力。

泛读窗口只读「认识型」章节（`01-1` / `01-4` / `06-2`），读完写 3 行概念卡，**不写代码**。它是给你「想多知道一点」的需求留的出口，不占主线时间。

**疲劳安全阀**（详见「一」的仲裁规则）：连续 2 天坐下先刷 30 分钟手机 / 任务做完但讲不出自己写了什么 / 周中就开始盼周末 —— 出现任一，**降到 20h/周**（健康优先）；若同时进度落后，按「一」的砍单阶梯砍内容，不砍死线。

## 五、资源清单（只用这些，别再找第四个来源）

- 廖雪峰 Python 教程 https://liaoxuefeng.com/books/python/introduction/index.html
- Python 官方中文教程 https://docs.python.org/zh-cn/3/tutorial/index.html
- uv 文档 https://docs.astral.sh/uv/
- DeepSeek API 文档 https://api-docs.deepseek.com/zh-cn/ （模型名 `deepseek-flash` / `deepseek-v4-pro`）
- 通义千问（备用，有免费额度）https://help.aliyun.com/zh/model-studio/ （base_url `https://dashscope.aliyuncs.com/compatible-mode/v1`，模型 `qwen-plus`）
- FastAPI 文档 https://fastapi.tiangolo.com/zh/
- Chroma 文档 https://docs.trychroma.com/
- LangGraph 文档 https://langchain-ai.github.io/langgraph/
- 中文 Agent 系统教程（RAG/LangGraph/面试题，2026 更新）https://github.com/didilili/ai-agents-from-zero
  —— **两条轨道**：① 精读（项目驱动，占 95% 时间）遇到问题去查对应章节；② 泛读（每周日 30 分钟）只读认识型章节 `01-1`/`01-4`/`06-2`，只写概念卡不写代码。映射：W3-W8 用 `01-1`/`01-3`；W9-W16 用 `04-1`~`04-5`；W17-W20 用 `03-1`/`03-2`；W21 用 `03-3`（MCP）；W22 用 `01-2`（Ollama）；跳过 `02`（Coze/Dify 低代码）、`05`（微调）、`03-4`（A2A，第 6 月后）。详见 `notes/README.md`
- 面试向 RAG+Agent 速通 https://github.com/limouren2000/llms-dev-study

## 六、当前坐标

> 更新于 2026-09-22 15:25（**W4 进行中 + 排期实测校准**）。**这里是「实际到哪」，不是计划。**

- **阶段**：**阶段② 大模型 API · W3 已通过**（2026-09-21）→ **W4 进行中**：第 1 件（多轮上下文）09-22 已写完待验收，剩余 3 件（流式 / 四个命令 / token 累计）见 `handbook/07`
- **排期实测校准（2026-09-22）**：W3 全格实测 **2h55**（预算 5 天）；W4 第 1 件实测 **1h20**。**里程碑不动**，省下的时间投进 `ROADMAP` 「二·一、深度要求」；W4-W7 预计 10-05 前后收完，到 W8（10-30 项目①）的空档**明确用于项目①深度打磨**。根因：旧版 `.hermes.md` 硬规则 3「一次只给一个任务…提前做完就停」在制造少任务，已改为「按 3-4 小时配量、一次 2-3 件」
- **死线**：内部目标日 2027-02-01（距 2026-09-22 还有 **132 天 ≈ 18.9 周**）；毕业 2027-06-01。**天数只在「一、总目标」和这里各写一次，改动时必须两处同步**（2026-09-22 曾出现 136/133 两个数并存）
- **毕设（2026-09-21 选题会已定）**：**#91「基于大语言模型与 RAG 技术的企业制度智能检索与问答系统」，导师林敏** —— 与项目②同构，**零额外工作量**，语料收窄为「企业制度文档」（只影响 W9 数据准备）；答辩/开题/中期日期待学校通知
- **投入**：28 h/周（起步两周 25h 试运行，10/5 复盘）；里程碑见「二·二」
- **W1 交付**：`week01/contacts.py` 单文件（`add`/`list`/`find`/`del`/`quit` + 校验 + 越界兜底 + 序号），commit `1a4ce70`→`7c9add4`（该文件已删，git 历史可查）
- **W2 交付**：`week01/contacts/{storage,core,cli}.py` 三层 + 全部类型注解 + `pathlib`；`test_contacts.py`（9 条原生 assert）；`load_contacts` 三层结构校验（畸形 json 不崩、不覆盖原文件）；`except EOFError` 退出存盘；`ruff` 全仓通过；`contacts.json` 移出版本库；`week01/contacts/README.md` 已与代码对齐；`week02/read_env.py`（`load_dotenv` + `os.getenv`，只打印长度不打印 key）+ `week02/first_request.py`（`httpx.get` + 先判状态码再取字段）
- **W3 交付**：`week03/hello_api.py`（中转端点 + `deepseek-flash` + 关思考模式；`try/except/else` 四类错误分类含 402/429；`usage` 三个 token 数 + 单次费用核算，算式与平台 `cost_cny` 逐位吻合）；`week02/first_request_async.py`（异步版 `httpx`）；`week01/contacts/cli.py` 改 `try/finally` 覆盖全部退出路径（Ctrl+C 实测存盘）
- **W3 验收（2026-09-21 总指挥实跑）**：4 项任务全过；**学员当场吊销了泄露的旧 key**（旧 key 实测已失效）；本仓库 `git log --all` 全扫描确认 `.env` 从未进入任何提交。**未覆盖**：`temperature`（归 W5 温度实验）、流式代码与 `chat_cli v0.1`（顺延 W4）
- **W2 验收（2026-09-20 总指挥实跑）**：5 条标准中 **4 条满足**；未满足 1 条 = 「意外退出（EOF/关窗口）不丢数据」—— `EOFError` 已修，**Ctrl+C（`KeyboardInterrupt`）与进程被 kill 仍丢数据**，已挂到 W3 第 1 件（`try/finally` 一次覆盖全部退出路径），不卡住 W2。
- **W2 暴露的体系漏教**：`grep` 全仓确认「git 撤回 / 删文件」从未进过课程（学员 A 题诚实答「你没教过」，答得对）→ 已列入 W3 第 2 件。
- **W2 结束要能**：不看代码回答「改一个功能要动哪个文件」「把 json 换成数据库要动哪几个文件」—— 前者已过；**分页序号归属** 09-21 复验通过（正解：core 必须能返回全局真实序号，cli 只决定显示），此项关闭。
- **W3 进行中（2026-09-21）**：起步四格当日全部完成 —— ① `cli.py` `try/finally` 覆盖全部退出路径（commit `82edb3e`，Ctrl+C 不再丢数据）；② git 撤回补课 + key 吊销演练（临时仓库完成，复验通过）；③ `week03/hello_api.py` 首调大模型 API 成功（非流式 + 四类错误分类 + 429 分支 + 单次费用核算 `0.000094 元` 与中转 `cost_cny` 逐位吻合，commit `57a4cf8`）；④ `week02/first_request_async.py` async/await 认识（W6 前置）。**主用端点已换 tokenrhythm 中转**（官方账号欠费 402，key 存 `.env` 的 `TOKENRHYTHM_API_KEY`）。

概念定位与「什么不学」见 `notes/概念地图.md`；教程两条轨道见 `notes/README.md`；投递节点见 `docs/求职时间线.md`。
